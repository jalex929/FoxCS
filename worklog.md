# Worklog

Session-to-session continuity notes — what's mid-flight and what's next. Not append-only like `decisions-log.md`; update/trim this freely as work completes. See `decisions-log.md` for the permanent record of what was actually decided, and `open-questions.md` for longer-lived unresolved questions.

## Where things stand as of 2026-09-04 (later) — module structure settled, real rework implied for 01.5/01.6

See `decisions-log.md`'s matching entry for the full reasoning. Short version: 5 modules per lesson now (Instruction bundle / Project / Coding Exercise / Mastery Check / Feedback), Instruction goes back to freely-navigable tabbed HTML instead of native `mod_lesson` pages, Project and Coding Exercise are genuinely separate, submissions become file-only `.py`.

**Not decided yet — needs Jay's call before touching anything already live:**
1. Whether 01.5 and 01.6 (currently: Instruction and Practice as two separate native Lesson activities, no Project module at all) get reworked to match now, or stay as a grandfathered exception while new lessons follow the new structure going forward.
2. Whether 01.4's already-built Coding Exercise (and any other already-built assignment module) gets its submission settings changed to file-only `.py`, or only new builds get the stricter setting.
3. A real Project module has never been built for any Unit 01 lesson under this definition — first one needs to be authored from scratch (source: each lesson's `NN_project.html`/`NN_project.py`), not just re-labeled from existing content.

## Where things stand as of 2026-09-04 — completion/telemetry prototype proven in the sandbox

See `decisions-log.md`'s matching 2026-09-04 entry for the full reasoning (SCORM vs. full-native-rebuild vs. this). Short version: the "01.3 can't be marked done" bug is a real ceiling in `mod_resource`, confirmed in Moodle's own source, not a config issue. Fixed with a small custom plugin (`local_foxcstelemetry`, source in `07-infrastructure/local-plugins/foxcstelemetry/`, deployed to `/var/www/moodle/public/local/foxcstelemetry/`) that logs telemetry and calls Moodle's own generic completion API — proven end-to-end against a real logged-in `foxcstest` session in `sandbox-adaptive-demo` (course id 9, cmid 235): telemetry row landed in `mdl_local_foxcstelemetry_log`, and `mdl_course_modules_completion` shows real completion, not just a JSON success response.

**Next up, in rough order:**
1. **Pick which real lesson gets this first** — not decided. Candidates: Python 01.1–01.3 (the lessons actually stuck at "can't mark done" today), or wire it into the 01.5/01.6 tabbed-Lesson-page pattern instead for finer telemetry on embedded interactions (that pattern's overall Lesson completion already works natively — see decisions-log — this would only add value for telemetry on custom JS *within* a Content page).
2. **Decide the XP/grade path** — deliberately not built yet. Either push a score through the gradebook from `log.php` directly, or leave it to the autograder reading `mdl_local_foxcstelemetry_log` (matches the already-decided "autograder scans Moodle directly" priority) — not decided which.
3. **Clean up the sandbox prototype** once Jay's reviewed it (cmid 235 in `sandbox-adaptive-demo`) — currently left live for him to look at, not meant to stay as permanent sandbox content.
4. Resolve the still-open "correct the N mistakes" bug in `courses/python/content/unit_01_what_is_programming/lesson_01_06_common_syntax_mistakes/07_mastery_check.html` line 197 (`Print(Continue` — says "two mistakes," actually has three: capitalization, missing quotes, missing paren) — flagged, not fixed; needs Jay's call on which to treat as "the" two, or reword vague per the new `content-authoring-standards.md` rule.

## Where things stand as of 2026-08-06 (consolidated — see `decisions-log.md` for full entries on each)

**Moodle is paused, not abandoned.** FoxCS pivoted to an MVP: self-contained unit folders, distributed and submitted through Google Classroom, carrying the same instructional logic (DOK spread, Reinforce/Core/Extend ladder, objectives) the Moodle plan was built around. `CLAUDE.md`, `02-authoring-system/mvp-unit-folder-structure.md`, and `courses/python/course-plan.md` all reflect this.

**Course catalog** (still forming): 4 courses — Game Programming I (Python, sequential, active), Game Programming II (JS/HTML5 app dev and/or Unity, student's choice), Web Dev/"Web II" (same independent-exploration model, HCD focus, PHP-ish backend). Whether "Unity" is a separate course or a Game II lane is unresolved — see `open-questions.md`.

**FoxCS: Python has a year-long game-design/MDA/journal thread** woven through all 21 units (`course-plan.md`'s "Game Design, UX, and Journal Threads" section) — MDA framework, journal prompts growing from 50-75 words (Unit 00) to a ~2-page paper (Unit 20), a confirmed grading rubric, and a platform-wide AI-use academic-integrity policy (`01-privacy-and-governance/academic-integrity-ai-use.md`: AI-generated writing/code = 0 + Aspen record, always human-confirmed first).

**Unit 01 exists in two different states right now — know which lesson you're looking at:**
- **Lesson 01.4** (`lesson_01_04_printing_output/`) is the **current reference implementation**: flat numbered files `01_instruction.html` through `11_feedback.html` (11 steps, no subfolders), mastery-check/project answers in plain `.py` files saved via VS Code, Duolingo-style disposable practice drills, tiered project XP, a drag-to-match vocab quiz gated on 100% completion (saves with a `_completed` filename suffix), an HTML feedback form using save-in-place, AA-corrected colors per the new `design-system.md`. Built 2026-08-06 after researching and rejecting Monaco/Pyodide as overkill (students already have Python via VS Code) — see `decisions-log.md`'s two 2026-08-06 entries for the full reasoning, including a couple of features (flashcard self-assessment, obfuscated mastery-check content) that were built and then deliberately dropped in the same session.
- **Lessons 01.1, 01.2, 01.3, 01.5, 01.6** are still on the **prior (2026-08-04) engine**: stateful practice-routing JS, mastery checks with embedded answer textareas + save-in-place, old un-corrected colors (`#2a78d6`/`#eda100` fail AA contrast with white text — see `design-system.md`), multi-variant password-gating only exists on 01.1. Don't treat these as the current pattern.
- The Unit Project, journal (in `lesson_01_02.../`), and codename-policy/naming decisions (no self-naming, numbered-not-subfoldered files) apply across all 6 lessons already.

**Added later the same day (2026-08-06), several more rounds:**
1. A drag-to-match vocab quiz (`04_vocab_quiz.html`) as the real XP-earning flashcard-study activity — save gated on getting all terms correct, filename gets a `_completed` suffix on save. This bumped every later file in Lesson 01.4 up one number (now `01` through `11`, ending at `11_feedback.html`).
2. An indentation-aware two-line "sneak peek" block builder previewing Unit 05's `if` statements (Drill 7 on `05_practice.html`, clearly bonus/not-core-content since Unit 01 doesn't teach indentation-requiring syntax yet), and a combined dropdown+typed-blank drill (Drill 6, same file). Also surveyed all of Unit 01 and identified two more drag-drop types worth building later — categorization (01.2's I/P/O sort, 01.6's error-type sort) and sequencing (01.3's line-reordering).
3. **A video embed was added to `01_instruction.html`, then removed the same day.** Jay clarified he didn't want fabricated content in real lessons, just proven functionality — confirmed hard by the placeholder producing a real YouTube Error 153 when tested. Replaced with **`02-authoring-system/component-library/index.html`** — a real, browsable HTML page cataloging every interactive pattern (12 total) with working demos using deliberately generic, non-curriculum content. This is also where the categorization and sequencing types got their first real, working implementation (demoed there, not yet built into any real lesson) — Jay can review and approve the mechanic before it's invested into 01.2/01.3/01.6.

See `decisions-log.md`'s matching same-day entries and `mvp-unit-folder-structure.md`'s "Component Library" section (which now contains "Interactive Drill Types" and "Embedded Video" as subsections).

Marked ✅ drafted, not 🔍 reviewed, in `course-plan.md` — none of this has had Jay's eyes on it yet as a real class experience.

## Added 2026-08-07/08 — component library review pass

Jay reviewed the component library and flagged real fixes, all applied: multi-line block builder now uses valid, labeled Python instead of ambiguous pseudo-syntax; feedback across most components now explains *why* an answer is right, with a general-reminder fallback for genuinely undiagnosable wrong answers (typed free-recall especially); drag-to-match shows a labeled badge for which term landed on which definition; categorization has real text feedback instead of a silent counter; sequencing has ▲▼ button backups to dragging; glossary card clamps to the viewport. Video embed is now **Live** — Jay provided a real video and it's embedded in the library for the first time, first real proof the pattern works (not yet deployed into any real lesson). Full detail in `decisions-log.md`'s 2026-08-07 entries.

**Mastery-check timestamp mechanism corrected**, both in the component library (#12) and the real `lesson_01_04_printing_output/09_mastery_check.html`: students no longer copy a visible timestamp by hand (a mistake in the original design, per Jay) — unlock/completion times now write automatically as hidden DOM fields and save with the page itself. `mvp-unit-folder-structure.md` and the teacher-materials KEY doc (which also had a stale `08_/09_` file-number reference from an earlier renumbering pass) are corrected to match.

**Resolved:** simulated in-browser IDE that matches typed code against accepted responses — declined, same reasoning as the earlier Monaco/Pyodide rejection. **Built instead:** component #14, a code execution stepper (simulated IDE look, syntax highlighting, Play/Pause/Step/speed transport, a curated-branch dropdown for "what if" input, and a separate randomized-speed typing animation for debugging/edit demos). Nothing executes — every step is hand-authored. Refined same day: speed control is now a 0.25-2.0 quarter-step slider, and all three traces now include a 4th "after the loop" line showing execution continues past the loop regardless of iteration count. Not yet deployed into any real lesson.

**Shared CSS + design system built** — `02-authoring-system/shared-styles/` (`foxcs-base.css`, `foxcs-ide-dark.css`, `foxcs-theme-toggle.js`, `README.md`). Component library now links these live instead of repeating its own copy, with a working light/dark toggle. Real lesson content intentionally does NOT link them yet — see `shared-styles/README.md`: the open question of whether Classroom preserves folder structure on download makes an external `<link>` a real risk for distributed content, so lesson pages stay self-contained and hand-synced for now. Full detail, including real computed contrast ratios for the new dark theme, in `decisions-log.md`'s 2026-08-08 entries and `design-system.md`'s new "Dark Mode / Theme Toggle" section.

## Added 2026-08-08 (later) — theme system + telemetry, design only

Jay wants 4 student-selectable color themes (light default, dark, new Natural/parchment, new Synthwave/cyber — palettes pending his own visual reference) and a full interaction-telemetry layer (theme choice, every stepper speed change, hints used, Core/Reinforce/Extend routing per skill, stuck/recovery evidence, start/save timestamps), aimed at markdown/JSON files he can later load into a database. Two new design docs written, nothing built yet: `02-authoring-system/theme-system.md` and `02-authoring-system/telemetry-and-analytics.md`. See `decisions-log.md`'s matching entry for the full design (link-edit + dropdown reconciled at Save; one hidden JSON event log per page; pipeline rides the existing not-yet-built codename-swap script).

**Reopened, not resolved:** the Classroom-folder-structure-on-download question now blocks three things (shared CSS rollout, the codename-swap script's scoping, and this theme system). Worth Jay running the cheap empirical test described in `theme-system.md` before more is built around the unknown.

## Added 2026-08-08 (later still) — theme system + telemetry wired into the component library (#15, #16), browser-tested

Both designs are now real, working demos, not just docs: 4 new theme files (`shared-styles/foxcs-theme-{light,dark,natural,synth}.css` — Natural/Synthwave explicitly placeholder), the reusable `shared-styles/foxcs-telemetry.js` event-log mechanism, and a new `component-library/theme-and-telemetry-demo.html` showing both working together (theme dropdown + link-edit reconciled at Save, a throwaway Core/Reinforce/Extend drill generating real events, a toggle-able panel showing the live telemetry JSON). Embedded into `component-library/index.html` as #15/#16. Browser-tested end to end (local server + Chrome automation) — no console errors, all 4 themes render, both theme-change methods fire events correctly, the Reinforce path and "stuck" flag both work. Full detail in `decisions-log.md`'s matching entry.

**Next up, added to the existing list:** wire `FoxCSTelemetry.log()` into the *other* library components (stepper transport controls, drag-to-match, categorization, sequencing, hints) — not done yet, this pass only proved the mechanism on a standalone demo.

## Added 2026-08-10 (even later) — block builder layout, color fixes, misconception redesign + reveal, Key Terms analogies, vocab quiz reflection

A long stretch of concrete fixes across both the theme system and real Lesson 01.4 content, all browser-tested: multi-line block builder now shows both target lines together before the piece banks (component-library #3 + Drill 7); Light/Dark's Warning and Error colors were too similar, now genuinely distinct hues; Synthwave's violet accent extended to drag-drop borders and "already placed" blocks (feedback colors themselves confirmed and left untouched); the "Two Mistakes" section restyled from a heavy red block (which turned out to be a real Tone Note violation — red is reserved for academic integrity) to a plain reading card, with a new "click to see the fixed version" reveal showing the diff in red; every Key Term now has an Example/Note and the more abstract ones get a real analogy; and a new optional reflection prompt at the end of the vocab quiz asks about memory tricks. Full detail in `decisions-log.md`'s matching entry. Also flagged for later, not built: overprovisioned block-builder banks with real distractors, and typed (not just dragged) blanks.

## Added 2026-08-10 (later) — texture tuning + real Lesson 01.4 content fixes

More live review, two threads: the theme specimen's texture needed more presence and to scroll with the page (now 0.1 opacity, yellow-gold tint, `position: absolute`), and a real contrast bug surfaced in inline `<code>` nested in colored feedback boxes (fixed — explicit `color` instead of inheriting). Separately, Jay reviewed actual Lesson 01.4 content and flagged real issues, all fixed: the "Two Mistakes" section's first example was silently teaching two errors as one (fixed to isolate the quote issue); bolded `Label:` lead-ins now always start their own line (new standing rule in `content-voice-and-tone.md`); Key Terms redesigned from indented `<dl>` to non-clickable-looking cards; the passive "Before You Submit" checklist moved from the instructional page (where students couldn't act on it) to sit right above the first practice drill as a reminder; and a new Drill 8 gives students real practice diagnosing a compound error (missing both the quote and the paren at once), separated from the now-isolated teaching example. Full detail in `decisions-log.md`'s matching entry, browser-tested end to end.

## Added 2026-08-10 — full contrast rebuild, background textures, per-theme body fonts

Jay's review of the live specimen (real WCAG math, not eyeballing) found the feedback boxes in Natural/Synthwave were only ~1.2-2.1:1 against their own page background — technically AA-passing text, but not reading as a distinct surface. Rebuilt both themes' feedback as rich chips (now ~5.4-9.6:1 vs page, AAA text everywhere), lightened Natural's background again, gave Synthwave's Accent badge its own violet hue instead of reusing Error's magenta ("expand the palette"), and built real background textures (SVG paper grain for Natural, CSS line grid for Synthwave, both using the style guide's own opacity numbers). Also relaxed the "body text is always Atkinson Hyperlegible" rule — Natural now reads in Lora, Synthwave in Sora, both self-hosted, free/OFL, genuinely legible text faces. Full numbers and reasoning in `decisions-log.md`; `theme-system.md` updated to match. Production theme files (`shared-styles/foxcs-theme-natural.css`/`-synth.css`) carry all of this now — the component library and telemetry demo pick up the new colors automatically since they link those files directly.

**Not yet done:** wiring the new font tokens into `foxcs-base.css` itself (still hardcodes Georgia) or the background texture into any real component — both exist only in `theme-typography-specimen.html` so far.

## Added 2026-08-09 — Code Hotspot component (#17)

New component-library pattern: hover/click annotations on specific code tokens (why is this keyword here, not what does it execute), plus an ordered step-through slideshow sharing the same state — both requested by Jay mid-session, using his own C# `public`/`private`/`void` example. Reuses the dark IDE surface from the code stepper (#14) and the hover/pin mechanism from Glossary Term (#13) rather than building either from scratch. Browser-tested (click-to-pin, step transport, sync between the two modes) — see `decisions-log.md`'s matching entry. Not yet deployed to any real lesson; natural fit once Game II/Unity content exists.

## Added 2026-08-08 (even later) — real Natural/Synthwave palette, self-hosted fonts, typography specimen

`logos/waypoint_theme_typography_style_guide_full.md` (Jay's real spec, replacing the earlier placeholder guesswork) is now implemented: `shared-styles/foxcs-theme-natural.css`/`-synth.css` carry the guide's real palette, `shared-styles/foxcs-fonts.css` + `shared-styles/fonts/` self-host all 4 required fonts (Atkinson Hyperlegible, Nunito Sans, JetBrains Mono, Source Sans 3 — all free/OFL, confirmed downloadable from fonts.google.com), and `theme-typography-specimen.html` is a real toggleable page showing all 4 themes' typography and color palette side by side. Jay reviewed the rendered page live and asked for 3 fixes, all applied and re-verified in-browser: Natural's background lightened (was reading too tan), Synthwave's Border Strong swapped to the brand magenta for real pink presence (plus a new badge pattern using the guide's functional magenta), and every dark-background theme's semantic feedback colors brightened so they read as distinct surfaces instead of nearly blending into the page. Full reasoning in `decisions-log.md`'s matching entry; `theme-system.md` updated to match.

**Resolved, not still "Waiting on Jay":** the "updated visual-generation reference" item below — it arrived and is now implemented. What's left: real background texture for Natural (not attempted yet) and Jay's continued visual review as the logos themselves finalize.

## Waiting on Jay

- **`02-authoring-system/image-style-guide.md` (conceptual illustration palette) still not updated** — `waypoint_theme_typography_style_guide_full.md` covers UI/theme colors and typography, not illustration style specifically. Don't build a visual illustration template around the current placeholder until Jay's illustration-specific reference lands. The Natural/Synthwave *theme* palettes themselves are resolved (see the entry above) — this is narrower, just the illustration/graphic-asset guidance.
- **Real background texture for the Natural theme** — `waypoint_theme_typography_style_guide_full.md` Section 10 describes the requirement (subtle parchment/speckled feel, low-opacity, never touching text contrast) but nothing is implemented; needs either a real texture image or a CSS-only approximation.
- **A real answer on whether Google Classroom preserves folder structure on download** — a cheap, one-time empirical test (see `theme-system.md`'s "Known Risk" section) that now unblocks three separate decisions at once.
- **Reaction to the Lesson 01.4 reference implementation** — this should decide whether it becomes the template for rebuilding Lessons 01.1-01.3/01.5-01.6, or needs adjustment first.
- **Whether Week 39 ("Final Game Jam Warm-Up — Mechanic redesign") is the design-your-own-game/MakeCode moment**, and whether it pairs with the Unit 20 capstone's already-planned design-document journal entry. Flagged, not confirmed.
- Real weekly Game of the Week pages haven't been built yet — the calendar unblocked this (see below) but nothing beyond the Week-4-adjacent template exists.

## Added 2026-08-06 — revisit/iterate design (Guess-the-Number spine + Game of the Week)

Two designs, confirmed with Jay, neither built into real lesson content yet:

1. **Guess-the-Number revisit spine** — connects three projects that were already independently planned (Unit 06 Number Guessing Game, Unit 11 Game of Chance, Unit 14 Safe Input System) into one continuous thread, seeded at Unit 05, with an optional bonus tail through Units 08-10/16/19. Python turtle graphics as a separate visual thread for Units 06-07 only. Full design in `decisions-log.md`.
2. **Game of the Week** — weekly, low-stakes game-analysis habit, deliberately not tied to unit content, distributed via Classroom (scheduled posts, not a second hosting site), reflection saved in place in the same page. Real 39-week dated calendar now in hand (Jay's `Sample Content/GAME OF THE WEEK CALENDAR (2026–27).pdf`, transcribed to `courses/python/game_of_the_week/game_of_the_week_calendar_2026-27.md`) — the "blocked on Jay's game list" item is resolved. Reference template at `_TEMPLATE_reference.html` (Rock-Paper-Scissors) is now close to real Week 4 content. No real weekly pages built yet.

Neither of these has been rolled into `course-plan.md`'s per-unit entries yet — that's the next real step once Jay confirms the spine design in practice (e.g., reacts to a built Unit 05/06 reference) and once the game sheet arrives.

## Resolved 2026-08-06, Sample Content review

Jay added real prior-year materials (`Sample Content/`) — reviewed in full, findings preserved in `00-project-overview/source-material/sample-content-review-2026-08-06.md`. Key outcomes: validated the numbered-file lesson structure against the real problem it fixes (the old U1L3 packet needed 6+ separate files, objectives duplicated 4x); adopted a complete, ready-to-use grading/feedback spec as `05-grader/feedback-and-grading-spec.md` (resolves the queued "sample teacher feedback responses" task); confirmed AI-use policy stays all-or-nothing to start the year, with a documented-use model as real but not-yet-active future intent. See `decisions-log.md`'s matching entry.

**Two real design opportunities surfaced, not yet built:**
- A FoxCS-native **project bank** (Prompt / What You'll Make / Core Skills / Creative Twist / Challenge Mode format, modeled on 3 real prior banks totaling 65 project entries) for embedded student choice.
- A concrete **"revisit and iterate" mechanic** — Jay's own example (teach input, later add a loop, first bounded-guess-count then guess-until-correct) has no prior precedent to port; needs fresh design, modeled loosely on the real Wasteland Adventure → Expansion Pack revisit pattern.

## Next up

1. **Roll the Lesson 01.4 pattern out to the rest of Unit 01** (01.1, 01.2, 01.3, 01.5, 01.6) — the natural next step now that the pattern is proven on one lesson. Not started. Include the two newly-identified drag-drop types while rolling out: **categorization** on 01.2 (Input/Process/Output sort) and 01.6 (SyntaxError vs. NameError sort), **sequencing** on 01.3 (reorder scrambled print() lines) — see `mvp-unit-folder-structure.md`'s "Interactive Drill Types" table.
2. **Build Unit 00: Course Onboarding end-to-end** — 7 lessons, no project, introduces the MDA framework + general game-mechanics vocabulary and the Unit 00 journal. Should use the Lesson 01.4 pattern from the start. Not started.
3. **Sample teacher feedback responses** — requested by Jay, explicitly queued. Per `content-voice-and-tone.md`'s What-happened/Why/Next-step structure and `lesson-schema.md`'s `feedback.template` field.
4. **Scope and build the codename-swap-on-download script** — see `01-privacy-and-governance/codename-policy.md`'s "Tooling Needed" section. Blocked on knowing what Classroom's bulk-download format actually looks like.
5. **Grading engine, smallest real loop** — against Unit 01. Co-priority with content per Jay.
6. **Test save-in-place on real school Chromebooks**, especially over `file://` — not yet done, see `05-grader/README.md`'s Testing Needs.
7. **Not yet started, lower priority:** whether Game II/Web II need a different (menu/choice-board, not sequential-unit) folder shape — see `open-questions.md`.
8. **Not done, flagged rather than attempted:** a full prose pass of Unit 01 against every rule in `waypoint_curriculum_copywriting_guide.md` (cognitive-load sequencing, feedback-progression staging, etc.) — only the concretely-requested pieces were applied so far.

## Superseded by the pivot — not being done for now

The 2026-07-24 plan was to sample lesson 01.4 side-by-side across Moodle H5P, the Moodle Lesson-activity ladder, and VS Code, plus research whether Moodle can host runnable Python. On hold along with the rest of the Moodle side — worth returning to once Moodle resumes, not deleted from history.

## Longer-term, not urgent

- Pool-size math for the Reinforce/Core/Extend ladder vs. per-unit practice caps needs pressure-testing against real classroom use.
- VS Code's role under the MVP isn't fully settled — still a distinct "surface," or just the editor for the files that need one? (see `open-questions.md`).

## Where things stand as of 2026-08-20 — all committed, nothing pushed

Four commits landed this session, none pushed yet: `583d1eb` (grader), `5d7dcbc` (nav/section redesign + doc), `5165f32` (new vocab quiz component), `6523ece` (wiring + end-to-end grader proof). Plus the two rollout-fork commits from 2026-08-18 (`126913d` Level 1, `1d73708` Level 2) underneath those.

**Grader (`05-grader/`): built, tested, documented, and now proven against real non-Python content.** `school-side/auto_grade.py` (stdlib Python, no installs) scores the `_completed`-suffix + `foxcs-telemetry` JSON convention; `needs_review_manifest.csv` flags everything needing Claude Code's judgment at home. 21-test suite, CI workflow. **New:** ran it against a real completed submission generated by actually driving Lesson 0.5's new vocab quiz through its JS (not hand-guessed) — correct attempt counts, first-attempt-vs-final-save correctness, and reflection text, all with **zero changes to the grader script**. First real proof the two-tier architecture generalizes beyond Python.

**Unit 0 nav/section redesign: fully landed, both editions, including the sub-page (quiz) pages.** Sticky "Unit 0 Menu" dropdown, localStorage checkmarks, boxed sections with bold labels, "group" → "section" throughout. The Kickoff Activity inline-list gap (both overview pages) is fixed. `02_vocab_quiz.html` also carries the same top menu now — its own entry (0.5) is bold/"current" while sitting on that page.

**Lesson 0.5's vocab quiz: built, bug-fixed, wired in, verified end to end, both editions.** Real 4-term matching quiz reusing the Python-established telemetry pattern. Its completion checkmark is keyed to an actual successful save (gated the same way the Save button itself is gated: all four matched + reflection filled), not just opening the reading page — a deliberate, documented rule (`lesson-navigation-standards.md`'s "Interactive Checks" section) for any lesson that gets one of these going forward. Level 2's copy verified to differ from Level 1's only in the expected edition-specific spots (menu wording, storage key, the extra "Choosing Your Path" section) — diffed to confirm nothing else drifted.

## Next steps (pick up here)

1. **Push.** Nothing from this session is on the remote yet — `git log --oneline -8` to review, then push when ready.
2. **Scope the rest of Unit 0's interactive checks.** Jay confirmed some of this can live as lighter inline quick-checks in the reading itself (0.5's `01_instruction.html` already has one, ungraded) rather than every lesson needing a full standalone quiz page. 0.8 Academic Integrity is the next strong candidate for a real standalone check, given the stakes — decide the rest lesson-by-lesson rather than blanket-building quizzes everywhere.
3. **Google Doc / styled-worksheet path confirmed viable, not yet a real workflow.** Demoed generating a worksheet as paste-ready HTML (`Ctrl+A`/`Ctrl+C` from a rendered page, paste into Docs) — no direct Docs API in this environment, so this is the path going forward once Jay has a real worksheet need and/or a style guide to match.

## Where things stand as of 2026-08-29 — Seminar III slide/deck audit; resuming Moodle + domain

**Purpose of this session so far:** start a lightweight per-task worklog habit (task, purpose, done-vs-not), audit how much of the Seminar III instructor deck rebuild is actually finished, and pick back up on the Moodle build — specifically connecting the domain Jay just registered. This file already serves the worklog purpose; continuing to append to it rather than starting a parallel system.

**Seminar III teacher-materials decks (`teacher-materials/week-NN-presentation.html`): Weeks 2-9 exist, Week 1 doesn't (expected — see `project_seminar3_week1_schedule` memory, Week 1 has no full plan yet).** No deck has actually been rendered to PDF yet — `find` for `*.pdf` under `courses/seminar-iii` came back empty. The render tool (`tools/render-deck-pdf/render.js`, Puppeteer-based) is built and documented, configured to output **16:9 widescreen (13.333in x 7.5in, no margins)** for `slide` mode specifically for these decks; the decks' own CSS uses `vh`/`vw` viewport units rather than fixed pixel dimensions, so they're built to fit that render rather than authored against it and hoped-for. Actually exporting is a local-machine step (per the tool's own README — rendered PDFs are dragged into Drive by hand, not passed through chat) and hasn't happened yet for any week.

**Supporting student-facing materials are uneven relative to the decks** — decks exist for weeks 2-9, but `instructional-content/` and `printable-sheets/` only cover a subset:
- Weeks 3, 5, 9: instructional content + a full 4-file printable-sheet set (quick-reference, guided-practice, independent-practice, check).
- Week 4: instructional content only, no printable sheets yet.
- Week 7: instructional content + only a quick-reference sheet (missing guided-practice/independent-practice/check).
- Weeks 2, 6, 8: **deck exists, nothing else does** — no instructional-content page, no printable sheets.
- Answer keys: only Week 5 has one (`teacher-materials/week-05-answer-keys.html`).

**Moodle: resuming per the 2026-08-28 decision** (`decisions-log.md`). Confirmed this session that the Claude Code session in this repo is running directly on the `foxcs-droplet` (hostname `ubuntu-s-1vcpu-1gb-nyc1-08-23-26`, `/var/www/moodle` present) — the build/dev instance described in `07-infrastructure/moodle-vm-setup.md` is reachable and editable directly from here, not just documented. **Not yet done, and the actual next step:** Jay registered a domain and wants it connected. Per `moodle-vm-setup.md`'s Known gaps, the production host was never decided — this droplet was explicitly meant to stay build/dev-only (1.9GB RAM tier, ufw closed on 80/443, no TLS). Connecting the domain means deciding, before touching DNS: point it at this droplet (promoting it to production, which the repo's own docs currently argue against) or stand up a separate production host and point the domain there instead. Raised with Jay as an open question this session, not yet resolved.

## Added 2026-08-29 (later) — Moodle production host resolved to NTC Hosting; domain registration blocked on payment until tomorrow

**Production-hosting question (above) is resolved, not still open:** Jay already has an active NTC Hosting account (basic/shared plan), confirmed by Jay to be configured and sized for Moodle 5.2.2 despite being the basic tier — this becomes the real production host, separate from the `foxcs-droplet` build/dev instance. The droplet stays build/dev-only per the existing plan; nothing changes there.

**`foxcs.online` is still not registered** — confirmed twice now: once via direct RDAP query against Radix (the `.online` registry, showed "available for registration"), once by Jay directly ("it is hosted but not registered"). **Registering it is blocked on payment, which Jay can't do until tomorrow (2026-08-30).** Plan once paid: register the domain **through NTC Hosting itself** (same provider as the hosting account) so nameservers auto-configure — no manual NS pointing needed. Jay separately provided NTC Hosting's standing NS records (`dns.ntchosting.com` / `dns2.ntchosting.com` / `dns3.ntchosting.com` / `dns4.ntchosting.com`, default route `198.23.48.50`, shared SSL IP `162.210.96.119`) in case manual pointing is ever needed instead (e.g. registering elsewhere) — not needed for the current plan but kept here in case that changes.

**Browser automation is not available from this session** — Jay has the Claude in Chrome extension active locally, but this Claude Code session runs directly on the `foxcs-droplet` (confirmed via `hostname`: `ubuntu-s-1vcpu-1gb-nyc1-08-23-26`), a remote SSH-based session the local extension can't pair with. Confirmed via `ToolSearch` returning no `mcp__claude-in-chrome__*` tools in this session at all. **Any registrar/cPanel clicking-through in this session has to be a manual walkthrough** (Jay reports what he sees, gets told what to click) — browser-driven automation would need a Claude Code session running on Jay's own machine instead.

## Added 2026-08-29 (later still) — dev-instance Moodle course shells built for all 4 courses

Per Jay's request to "build moodle out a bit" ahead of hosting being ready: created one `FoxCS` category and 4 `topics`-format course shells on the dev instance (`foxcs-python`, `foxcs-game2`, `foxcs-webdev`, `foxcs-seminar3`). Python's 21 sections and Seminar III's 39 sections are renamed to their real unit/week titles pulled straight from `course-plan.md`/`01_SEMINAR_III_COURSE_PLAN` — Game II and Web Dev stay single-section skeletons since neither has a course-plan yet. **Seminar III also got its existing real content uploaded** as File resources (31 total) into the matching week sections — decks for weeks 2-9 (hidden from students, teacher-only, since they carry speaker notes), the one existing answer-key doc (also hidden), and every instructional-content/printable-sheet page that already existed for weeks 3/4/5/7/9. Nothing was invented to fill gaps — the upload exactly matches the audit above. Full writeup, the scripts, and a real gotcha (`/home/jay` isn't readable by `www-data`, everything has to stage through `/tmp` first) in the new `07-infrastructure/moodle-course-shells.md`. `CLAUDE.md`'s Seminar III status row was also stale (still said "Skeleton only... no content yet") — corrected to reflect what's actually built.

**None of this is committed to git yet** — `07-infrastructure/moodle-scripts/` (4 files) and `moodle-course-shells.md` are new/untracked, `CLAUDE.md`/`decisions-log.md`/this file are modified. Ask before committing, per standing practice.

## Added 2026-08-29 (even later) — Seminar III renumbered Week N → Unit (N-1), decoupled from the calendar

Jay wants to scope orientation + the first two real content weeks, which surfaced a naming question: he's moving Seminar III to stable **unit numbers** instead of calendar-week numbers, to support self-paced work (a student's progress shouldn't be pinned to a specific week of the year). Confirmed Seminar III-only — Python already uses `Unit NN`, Game II/Web Dev have no numbering yet. Mapping: old Week N → new Unit (N-1), matching Python's `Unit 00` start (Week 1 "Welcome to Seminar III" → **Unit 00**, Week 2 "Academic Problem-Solving" → **Unit 01**, Week 3 "Numbers & Operations" → **Unit 02**, etc.).

Since none of this was committed to git yet, did the rename properly rather than leave mixed terminology: both source docs (`01_SEMINAR_III_COURSE_PLAN`, `02_SEMINAR_III_ACADEMIC_CONTENT_MAP`) had every `Week N`/`Weeks N-M` heading and body reference converted; all 30 `week-NN-*` content files (decks, instructional content, printable sheets) renamed to `unit-(NN-1)-*` with in-file text updated too; the 8 top-level `week-0N-plan` source docs renamed to `unit-0N-plan` (367 in-file replacements); `render-deck-pdf`'s README examples updated; and on the dev Moodle instance, all 39 section names and all 31 already-uploaded resources' display names relabeled (section *number* untouched — content didn't move, only labels did). `populate-seminar3-resources.php` now matches `unit-NN-*` filenames for any future run. Full detail in `07-infrastructure/moodle-course-shells.md`'s new "Renumbering" section and `decisions-log.md`'s matching entry. Quarter date ranges (Aug 24 – Oct 23, etc.) were deliberately left alone — real grading-period boundaries, not part of what's being decoupled.

**Also clarified by Jay mid-session:** Unit 00's existing content ("Welcome to Seminar III" — course purpose, ACT overview, student profile survey, postsecondary intro) is considered done, no further scoping needed there. The actual next build task is a **Moodle student-orientation piece** — how to access work and navigate units/modules in Moodle — since that's new (platform didn't exist when Unit 00's content was originally written) and students will need it before Unit 01 opens.

## Added 2026-08-29 (final) — Unit 00 Moodle-orientation page built and uploaded

`instructional-content/unit-00-accessing-your-work-in-moodle.html` — new student-facing page, same visual component language as the rest of `instructional-content/` (banner/kicker, `problem-box`/`warn-box`/`check-grid`, quick-reference footer). Covers: what Moodle is, logging in (login URL is a bracketed placeholder — nothing real exists yet, see the domain/hosting entries above), finding the course from the Dashboard, the Unit 00/01/02... structure explained as self-paced and decoupled from calendar weeks (ties directly into today's renumbering decision), how to open a resource, and a reminder that ties back into the existing Monday check-in routine already defined in `01_SEMINAR_III_COURSE_PLAN`. Uploaded to the dev Moodle instance, visible, in Unit 00's section (section 1). No em dashes (checked against the standing style rule).

**Unit 00 is otherwise considered done per Jay** — its existing course-plan content (course purpose, ACT overview, student profile survey, postsecondary intro) doesn't need more scoping.

**Not yet done:** Unit 01 (Academic Problem-Solving, the week of Sept 1) still has only a teacher deck — no instructional-content or printable-sheets exist for it, unlike Unit 02 (Numbers & Operations, the week after) which is already fully built (deck + instructional page + 4 printable sheets). Scoping/building Unit 01's missing pieces is the natural next step but wasn't started this session — confirm with Jay before starting, since it's a real authoring task (a full instructional page + practice/check sheets), not a mechanical one.

## Added 2026-08-29 (truly final) — Unit 01 fully built out

Jay confirmed: build Unit 01's missing content. Six new files, drawn from the rich source material already sitting in `unit-01-plan` (1454 lines, previously `week-02-plan`, never fully used until now):

- `instructional-content/unit-01-what-do-i-do-when-im-stuck.html` — the five-question problem-solving routine (STOP/FIND/CONNECT/TRY/CHECK shortcut), a worked math example and a non-math example, all five error types (Knowledge/Process/Execution/Comprehension/Strategy) with student-facing "what this sounds like" language and a next step for each, plus three worked error-classification examples. Deliberately scoped to concept-teaching only — the ACT Math baseline itself (`unit-01-plan` sections 21-29, a 20-24 question diagnostic with full metadata) is a separate, larger assessment-building task, not started.
- `printable-sheets/unit-01-quick-reference.html`, `unit-01-guided-practice.html` (worked examples + try-with-the-class problems, no answers shown), `unit-01-independent-practice.html` (13 new problems: 3 five-question-routine scenarios, 10 error-classification scenarios split evenly across all 5 error types, none overlapping with the instructional page's own examples), `unit-01-check.html` (the 10 ready-made understanding-check questions from `unit-01-plan` section 33, transcribed as-is, no answers shown).
- `teacher-materials/unit-01-answer-keys.html` (hidden resource) — full answers/reasoning for all three practice sheets above.

**Caught and fixed a real bug from the same day's renumbering pass** while reading the source docs for this: the regex-based Week-to-Unit conversion handled single week references and dash-ranges fine, but silently missed the second number in constructions like "Weeks 3 and 4," "Weeks 3 through 8," and "Weeks 13, 20, 21" (only the first number was prefixed by "Week," so only it matched). Found by diffing every original `Week`-containing line (recovered via `git show HEAD:...` since these files were git-tracked) against the converted output — 8 total instances across `unit-01-plan`, `unit-04-plan`, `unit-06-plan`, `unit-07-plan`, `unit-08-plan`, and `02_SEMINAR_III_ACADEMIC_CONTENT_MAP`, all corrected by hand after confirming the original intent from context.

No em dashes or en dashes in any of the 6 new files (checked and fixed one stray en dash before upload). All uploaded to the dev Moodle instance's Unit 01 section — 5 visible, the answer key hidden.

**Unit 02 was already fully built before this session** (deck, instructional page, 4 printable sheets) — Units 01 and 02 are now both complete, matching what Jay actually asked to have ready for the week of Sept 1 and the week after.

## Added 2026-08-29 (yet again) — access-ease pass + made the dev Moodle viewable now

Jay asked to make Moodle content as accessible as possible once real students are on it, and separately to be able to view the dev instance himself before NTC Hosting is paid for. Checked three things against Moodle's real source/config before changing anything (avoided assuming and then "fixing" things that already worked): file resources already display embedded (not downloaded) for HTML, since `resource_get_final_display_type()` auto-resolves `.html` mimetypes to `RESOURCELIB_DISPLAY_EMBED`; course layout already shows all sections on one scrolling page (`coursedisplay = 0`, Moodle's own default); Boost's Course Index side panel is on by default. The one real gap: admin (Jay) was not enrolled in any of the 4 courses, so they only appeared under Site Administration, not the Dashboard. Fixed with a new `enrol-admin-as-teacher.php` script.

**Reset the Moodle admin password** via `admin/cli/reset_password.php` — the value set at install time was never recorded anywhere, a real gap `moodle-vm-setup.md` already flagged, not something forgotten this session. New password given directly to Jay (not written here — see standing rule against writing secrets into the repo). Confirmed `ufw` still only allows SSH in (80/443 closed), matching the documented dev-instance posture, so viewing requires the SSH tunnel described in `moodle-vm-setup.md`'s Access section: `ssh -L 8080:localhost:80 foxcs-droplet`, then browse `http://localhost:8080`.

Full detail in `07-infrastructure/moodle-course-shells.md`'s new "Access-ease pass" section.

## Bug reported by Jay, 2026-08-29 — deck slide overflow in Moodle's embedded viewer

**Some presentation decks have text running off the slide when viewed embedded in Moodle**, and the embedded viewer itself has overflow/layout problems that don't look right, at least at laptop screen sizes. Not diagnosed yet — could be the decks' own fixed-viewport CSS (`vh`/`vw` units sized for the 16:9 PDF render, not for however Moodle's embed iframe/pane actually sizes itself) clashing with a smaller or differently-proportioned embedded viewport, or something in how Moodle's `RESOURCELIB_DISPLAY_EMBED` frames the content. Needs an actual laptop-sized viewport to reproduce and fix properly, not just a code read.

**Jay's plan:** on his desktop, use Chromium + Playwright to reproduce the overflow at real laptop screen sizes and fix the deck CSS accordingly. Not started this session.

## Direction change flagged by Jay, 2026-08-29 — static HTML checks/practice should become interactive H5P

**Two related concerns, both about how Unit 01/02's content actually behaves for a student, not just how it reads:**

1. **Resource hierarchy/organization within a unit needs work.** Right now every unit section in Moodle is a flat, undifferentiated list of File resources (deck, instructional page, quick reference, guided practice, independent practice, check, answer key) with no visual grouping by type or purpose. Jay wants this improved — likely something like labeled sub-groups (Read / Practice / Check) rather than one flat list, though the exact mechanism (Moodle Labels, subsections, a different course format) isn't decided yet.
2. **Checks and practice questions should be genuinely interactive, not static HTML a student just reads.** The Unit 01/02 check and practice sheets built this session are plain HTML: a student can look at a question and its four answer choices, but can't actually click an answer, get feedback, or have it recorded anywhere. Jay wants these rebuilt using **H5P** (Moodle's native interactive-content system) so students can actually answer, get instant feedback, and have it roll into the gradebook. This also means **adding inline understanding-checks inside the instructional content itself** (formative checks while reading, not just a separate check at the end).

**This isn't a new idea for FoxCS** — root `CLAUDE.md`'s Platform Decisions table already named H5P as Moodle's role ("conceptual layer + course home: video, H5P, guided practice, vocab, light adaptive support") and `00-project-overview/h5p-authoring-and-automation.md` already covers whether H5P content can be generated programmatically (yes, and how). It just never got applied to Seminar III specifically, since this course's check/practice content didn't exist until today and was built as static HTML to match the deck/instructional-content pattern already established for this course.

**Worth checking before rebuilding:** `02-authoring-system/component-library/` already has working hand-built interactive patterns (drag-to-match, categorization, sequencing, a vocab quiz) from the Python MVP-folder track — those were built as custom HTML/JS specifically *because* that track has no LMS backend to report to. Since Seminar III lives in real Moodle, native H5P content types (Question Set, Drag and Drop, Course Presentation with embedded questions) are the more natural fit and get gradebook integration for free — but it's worth deciding whether any of the component-library's existing interaction *designs* are worth mirroring in H5P rather than starting from a blank H5P authoring session.

**Not started.** Next real step is likely: confirm the H5P activity module works on this Moodle instance (it's core, already present per the earlier plugin check), then rebuild Unit 01's check as a first H5P pilot before doing the rest.

## Added 2026-08-29 (the real final one) — H5P pipeline proven, Unit 01 Check rebuilt as interactive content

Jay asked to actually see H5P working in the Moodle he can now view. Synced the H5P Content Type Hub for the first time on this instance (33 content types installed, including MultiChoice, QuestionSet, TrueFalse, DragText, DragQuestion, Blanks, CoursePresentation). Confirmed by reading Moodle's own validator source (not assuming) that a `.h5p` package only needs valid `h5p.json` + `content/content.json` when the target library is already installed server-side — no need to bundle library JS/CSS. Built `Unit 01 Check (Interactive)`, a real `H5P.QuestionSet` with all 10 `H5P.MultiChoice` questions from the static check, same stems/options/answers, verified against the answer key. Piloted as one question first, confirmed working via authenticated `curl` (library resolves correctly, all 10 questions present in processed content), then built the full version. Two new scripts in `07-infrastructure/moodle-scripts/`: `create-h5p-activity.php` (general-purpose, reusable) and `create-h5p-pilot.php` (the one-question proof, kept for reference). Full detail in `07-infrastructure/moodle-course-shells.md`'s new "H5P pipeline proven" section.

**Not done:** a reusable script that generates a `.h5p` package straight from question data (this first one's JSON was hand-built via a one-off Python script, not saved). The old static `unit-01-check.html` was left in place, not deleted — Jay's call on when to retire it.

## Added 2026-08-29 (still going) — Unit 01's instructional content rebuilt interactive, with sequencing

Jay asked to see the instructional content itself made interactive too, and separately asked for real sequencing/reordering interactions where they fit, including a real-world applied example (not just the abstract 5-question order). Rebuilt `unit-01-what-do-i-do-when-im-stuck.html` as an `H5P.Column` mixing 6 reading blocks with 2 `H5P.MultiChoice` and 1 `H5P.TrueFalse` checks placed at natural pauses, not just at the end. `H5P.SortParagraphs` ("Sort the Paragraphs") was the right existing library for sequencing.

**Caught a real bug mid-build:** first attempt nested the sequencing blocks inside the Column and Moodle silently rejected them ("library ... is not valid") — turns out being installed/enabled site-wide isn't enough, a wrapper type like Column or QuestionSet only accepts sub-content types explicitly listed in *its own* semantics.json options, and `H5P.SortParagraphs` isn't listed in either. Fixed by pulling sequencing out as 3 separate standalone activities instead of nesting: reorder the five-question routine, reorder the order-of-operations steps, and a new real-world scenario (afford $45 shoes vs. saving $20 for a gift) sequencing the concrete applied steps, not just the abstract questions. All verified error-free and content-correct individually. Full root-cause writeup and the general lesson (check the wrapper's own options list, don't assume site-wide install is enough) in `07-infrastructure/moodle-course-shells.md`.

Unit 01 now has 5 H5P activities total: the Check (QuestionSet), the instructional Column, and 3 standalone sequencing exercises — alongside the original static HTML files, which were left in place throughout.

## Added 2026-08-29 (yes, still) — real SVG diagrams added for visual learners

Jay asked for a "breaking it down" visual treatment of the problem-solving sequence, plus SVGs generally wherever they help visual learners. Hand-drew two diagrams: a five-step flow of the routine (STOP/FIND/CONNECT/TRY/CHECK, numbered circles, each with its real question and a short example) and an icon-card set for the five error types (lightbulb/looping-arrow/off-center-target/question-bubble/signpost). **Found a real constraint:** H5P's content-file whitelist doesn't include `.svg` at all (confirmed in `H5PCore::$defaultContentWhitelist`, and Moodle doesn't extend it) — bundling a raw SVG in a `.h5p` package gets rejected at upload. Fixed by rasterizing to PNG with `rsvg-convert` (installed via `apt`, `librsvg2-bin`) before packaging; `H5P.Image` displays the PNG fine. Verified by fetching the actual served image bytes from Moodle's `pluginfile.php`, not just checking the reference was present. SVG sources saved into the repo at `07-infrastructure/moodle-scripts/diagrams/` so they're not stranded on the droplet's `/tmp`.

## Added 2026-08-29 (final final) — arrow fixed, error-type testing added, split into two readings

Jay's feedback on the interactive instructional build, all three addressed: (1) the Process icon's arrow pointed the wrong way — recomputed the arc/arrowhead geometry properly rather than eyeballing it, fixed in the SVG source and regenerated; (2) "Learn From the Error" needed actual testing, not just three worked examples whose answers are given away in the text — added a 4-question "Test Yourself" block right after it, covering the error types not yet tested elsewhere with fresh scenarios; (3) split the one long Column into two shorter readings per Jay's suggestion — **Unit 01: Solving Problems (Interactive)** (6 blocks: the routine, its diagram, one check, the worked example) and **Unit 01: Error Types (Interactive)** (11 blocks: the five error types, the fixed diagram, the original check, the True/False, the worked examples, the new 4-question test, and a focused quick reference). The original single combined activity was deleted, not kept alongside the split. Both verified error-free with correct block counts.

## Added 2026-08-29 (one more) — STOP/FIND/CONNECT/TRY/CHECK labels inline with each question

Jay asked for the mnemonic word to appear directly with each question, not just as a separate recap line — e.g. "STOP: What information do I have?" instead of just "What information do I have?" Updated both the five-question routine list and the Walk Through a Problem worked example in `Unit 01: Solving Problems (Interactive)` to lead with the label. Verified all 5 labels present, correctly duplicated across both blocks (routine list + worked example), no processing errors.

## Added 2026-08-29 (truly, finally last) — per-answer feedback added to every question

Jay asked for feedback when students get a question right or wrong, not just a generic "Correct"/"Wrong" label. `H5P.MultiChoice` supports this natively via each answer's `tipsAndFeedback.chosenFeedback` field — text shown specifically when that option is picked. Added real, option-specific feedback (explaining *why* each choice is right or wrong, not just restating correct/incorrect) to all 16 multiple-choice questions across Unit 01's three H5P activities (10 in the Check, 1 in Solving Problems, 5 in Error Types), plus custom correct/wrong messages on the one True/False question. 64 feedback strings total for the MC questions alone. Verified counts match exactly (40 + 4 + 20) with zero processing errors after redeploying all three activities.

## Added 2026-08-29 (the actually last one) — checked tone against the real voice doc, found and fixed two things

Jay asked to check the H5P feedback's tone against the repo's actual voice/tone documentation rather than go by feel. Read `02-authoring-system/content-voice-and-tone.md` and Seminar III's own 15 authoring rules (`02_SEMINAR_III_ACADEMIC_CONTENT_MAP` section 50) directly. Found two real things, not just confirmed it was fine:

1. **Feedback structure didn't match the documented pattern** (what happened → why → suggested next step, no evaluative opener like "Wrong"/"Not quite" at all — the doc's own model examples never use one). Rewrote all MC feedback across all three Unit 01 H5P activities to drop "Not quite."/"Right." openers and add a concrete next step to wrong-answer feedback.
2. **Bigger finding: the voice doc requires "Learner" (capital L) in all curriculum copy, but the entire existing Seminar III corpus consistently used "student"** — not just my new feedback, every instructional page, every printable sheet, the course plan itself. Asked Jay how to resolve it since the scope went well beyond what I'd just built; he chose to bring Seminar III in line with the shared voice doc rather than treat "student" as an intentional deviation.

**Converted to "Learner"/"Learners"/"Learner's"** across all genuinely student-facing curriculum copy: every `instructional-content/*.html` and `printable-sheets/*.html` file (61 replacements, HTML header comments left alone since those are internal file-classification notes, not curriculum copy), plus all three Unit 01 H5P activities (question stems, feedback text). **Left as "student"** per the voice doc's own carve-out for internal/teacher-only material: `01_SEMINAR_III_COURSE_PLAN`, `02_SEMINAR_III_ACADEMIC_CONTENT_MAP`, the `unit-NN-plan` source docs, and `teacher-materials/` (decks + answer keys). Documented this scope boundary as a new rule 16 in Seminar III's own authoring rules so it doesn't need re-deriving later.

All three H5P activities redeployed and reverified: zero processing errors, zero remaining "student" references, "Learner" present throughout.

## Added 2026-08-29 (one more correction) — Learner capitalization fixed to normal sentence case

Jay caught that "Learner" shouldn't be capitalized every time, same as "student" never was — I'd over-applied the voice doc's "(capital L)" note as if it meant always-capitalized. Reprocessed all 61 replacements in the HTML files plus all three H5P activities with a sentence-position-aware fix instead of a blanket capital: "Learner"/"Learners" only capitalized when actually the first word of a sentence (preceded by nothing, or by `. ! ? :`, skipping over HTML tags to find the real preceding text), lowercase everywhere else. Manually reviewed all 61 HTML instances and all H5P instances in context — all correct (e.g. "A learner earns $15..." stays lowercase since "A" is the real first word; "Learners may use..." capitalizes correctly as an actual sentence start). Redeployed all three H5P activities, reverified zero processing errors.

## Added 2026-08-29 (genuinely the last one) — Unit 01 completeness pass + sequenced 01.1-01.11

Jay asked to make sure Unit 01 has everything needed for real practice, and to sequence the activities numerically. Built two new interactive QuestionSets for the error-classification halves of Guided/Independent Practice (5 and 10 questions, same scenarios as the existing printed sheets and answer key, same feedback pattern as everything else) — the open-ended "show your work" halves of those same sheets were deliberately left static, converting them to multiple-choice would misrepresent the task. Renamed and reordered all 11 student-facing Unit 01 resources with a `01.N --` prefix in real working order (Solving Problems through Check), and hid the two static resources now fully superseded by interactive replacements (old combined instructional page, old static Check) rather than leaving both versions visible and ambiguous. New script: `07-infrastructure/moodle-scripts/sequence-unit01.php`.

**Unit 01 is now complete except for the ACT Math Baseline** (20-24 diagnostic questions with metadata) — flagged again as the one real remaining gap, same scope note as before, not built this pass.

## Added 2026-08-29 (small cleanup) — dropped "(Interactive)" from all activity names

Jay asked to remove the "(Interactive)" suffix now that it's the norm rather than the exception in Unit 01. Renamed all 8 H5P activities (script: `07-infrastructure/moodle-scripts/remove-interactive-suffix.php`), e.g. "01.1 -- Solving Problems (Interactive)" -> "01.1 -- Solving Problems". Numbering/order untouched.

## Added 2026-08-29 (final, for real) — practice made typeable, H5P reference doc written

Two more requests: guided practice should be interactive (a worked example can stay static since nothing's being answered, but anything a Learner works out needs a real input field), and catalog the available H5P content types so future choices are deliberate.

Rebuilt 01.6 and 01.8 as `H5P.Column` activities using `H5P.Essay` for every "try these" problem — real typed input, a "Show sample solution" reveal with a full worked answer, worked examples left as static text per the rule. Hit and fixed a real bug: `create_module()` auto-appends new modules to the section's end, so swapping them into mid-sequence position left a duplicate trailing copy in `course_sections.sequence` — deduplicated, kept the correctly-positioned occurrence. Old static versions hidden and renamed `(old static version, superseded)` for clarity in the admin view.

Wrote `07-infrastructure/h5p-content-type-guide.md` — what's installed (~140 library records from the Hub sync), the hard rule about wrapper-specific allowlists (Column and QuestionSet each only accept sub-types explicitly listed in their own semantics, confirmed for both), everything actually proven working in Seminar III so far, and a curated (not exhaustive) list of what's worth exploring for later units — Chart for Data/Science, StructureStrip for Reading/English, BranchingScenario for the postsecondary-pathway content, ArithmeticQuiz for retrieval drills.

## Next up (added 2026-08-29)

1. **Build a reusable question-data-to-.h5p script** so future H5P activities don't need hand-written JSON per question. Then use it to rebuild Unit 01's guided/independent practice and Unit 02's check/practice as H5P too, and add inline understanding-checks inside instructional pages (H5P.CoursePresentation or embedding H5P.Column with questions).
2. **Improve resource hierarchy/grouping within each unit** (Read / Practice / Check labels or similar) — not started.
3. **Rebuild Unit 01/02's remaining checks and practice as interactive H5P content** (see direction-change entry above). Not started beyond Unit 01's Check.
2. **Fix deck slide overflow in Moodle's embedded viewer** (see bug report above) — Jay is taking this one himself with Chromium + Playwright.
3. **Build or scope the ACT Math baseline** for Unit 01 — 20-24 questions with skill/domain/difficulty metadata, per `unit-01-plan` sections 21-29. Not started; flagged as a separate, larger deliverable from the rest of Unit 01.
4. **Rotate the admin password again** before any real student data touches this instance — the one set today is for Jay's own preview access only.
5. **Tomorrow (2026-08-30): register `foxcs.online` through NTC Hosting** once Jay can pay. Then verify propagation from this droplet with `dig @8.8.8.8 NS foxcs.online`.
6. **Confirm the NTC Hosting basic plan's actual specs** (PHP version, MySQL/MariaDB, cron access, memory limits) once logged in — Jay's already confident it's Moodle-5.2.2-ready, but worth a real look before installing, same diligence as the droplet build got.
4. **Install Moodle 5.2.2 on NTC Hosting** once domain + hosting are both confirmed live — likely via whatever the plan's control panel offers (cPanel/Softaculous or manual upload), TLS via the host's own tooling or Let's Encrypt if supported.
5. **Migrate the dev-instance course shells/content to NTC Hosting** once it's live — course backup (`.mbz`) restore per course is the documented path (`07-infrastructure/moodle-vm-setup.md`), not yet done.
6. **Render the existing Unit 01-08 Seminar III decks to PDF** using `tools/render-deck-pdf` — needs a local machine per the tool's own design, not this droplet session.
7. **Fill the printable-sheets/instructional-content gaps** for Units 01, 05, 07 (nothing built yet) and Unit 06 (missing 3 of 4 printable sheets) — each new file also needs a re-run of `populate-seminar3-resources.php` to land in Moodle.
8. **Game II and Web Dev have no course-plan.md or content at all** — both are still pure skeletons in the repo (per `CLAUDE.md`'s course table) and in Moodle (single default section, nothing uploaded). Scoping either one is a real content-authoring task, not a Moodle-setup task.
9. **Python has 20 of 21 units with no content** — only Unit 01 (6 lessons) exists; the Moodle course shell's sections are correctly named for all 21 but 20 of them are empty.

## Where things stand as of 2026-08-30

**`foxcs.online` registration is in progress, not yet live.** Payment cleared (confirmed via NTC billing: charged, next renewal Oct 28), but the domain still shows "Pending Registration" in the NTC client area and fails RDAP/DNS lookups — normal for a `.online` TLD, typically clears within a few hours. Once it flips to Active: verify DNS, log into the actual hosting control panel (not yet identified — cPanel/DirectAdmin/custom, Jay hasn't logged in yet) to attach the domain, check the real PHP/MySQL specs, and move to installing Moodle 5.2.2 there.

**Desktop/droplet git divergence found and reconciled.** A separate desktop session had pushed 3 unmerged commits (old Week-N-naming decks + a `student-materials/` folder) independently of this droplet's Unit-renumbering work. Merged per Jay's direction: desktop's redesigned decks (Units 01-04) and desktop's instructional content (all units) won; this droplet's own Units 05-08 decks stayed (already live in Moodle, never redesigned). Full detail in `decisions-log.md`'s 2026-08-30 merge entry. **Established going forward: commit and push routinely**, don't let droplet and desktop diverge like this again.

**Unit 01's Day 3-5 gap (item 3 above) is now closed.** The 24-question ACT Math Baseline, a pre-baseline rules-reference card, both Day 4/5 reflections, a teacher-only answer key/skill map, and a student+teacher "Week at a Glance" pacing sheet are all built and live in Moodle (`01.12` through `01.15`, plus the pacing sheet at the top of the section). Item 1 above (reusable question-to-.h5p script) is also now done — `07-infrastructure/moodle-scripts/h5p-builder/`. Full detail in `decisions-log.md`'s matching entry.

**Unit 01 consolidated from 16 to 12 visible Moodle items.** Jay flagged the sprawl and asked about a Unit->Lesson rename; scoped down to just fixing activity count (his call, rename explicitly deferred). Merged Guided/Independent Practice with their Classify-the-Error questions, folded the baseline Quick Reference into the baseline's own intro page, merged the two Day 4/5 reflections into one two-part activity. Full detail in `decisions-log.md`. `07-infrastructure/moodle-scripts/h5p-builder/merge_unit01.py` is a reusable template for consolidating other units later.

**NTC Hosting has a "Moodle Admin" folder already provisioned** — Jay found and downloaded `foxcs.online.zip` (142MB, likely the deployed Moodle codebase) and `moodledata.zip` (348 bytes, empty except for a `moodledata/foxcs.online/` skeleton, dated 2026-08-28) from the panel, saved to the SY27 Drive folder. Confirms the domain is already attached as a hosted/addon domain in the panel, ahead of registration finishing — the "attach the domain" step may already be done. Panel type still not identified (Jay wasn't sure what it's called). Domain still shows "Pending Registration" as of this check.

**Seminar III renamed Unit N -> Lesson N; orientation content is now unnumbered "Orientation."** Jay reopened the earlier-deferred rename, confirmed the scheme (Orientation unnumbered, not "Lesson 0"; letter suffixes A/B only when a lesson combines academic + postsecondary content, not needed anywhere yet). Applied everywhere: all 39 Moodle sections + every resource/activity name, all 41 repo files renamed `unit-NN-*` -> `lesson-N-*`/`orientation-*`, ~450 in-file text replacements across 44 files, `populate-seminar3-resources.php` and the h5p-builder scripts updated to match going forward. Full detail in `decisions-log.md`. Python's own Unit numbering is unaffected -- Jay confirmed this is Seminar III-specific.

**Domain registration is in manual review**, per an email Jay received from NTC/the registrar -- explains why it's taking longer than a typical few-hour `.online` registration. No action possible until that clears.

**Flagged, not yet acted on: Seminar III needs more postsecondary content than currently planned.** Jay: the course should have postsecondary content in most lessons -- **Lesson 1 is essentially the only one that shouldn't**, not the "light Q1 touch, real work concentrated in Q4" model `01_SEMINAR_III_COURSE_PLAN` and `project-seminar3-plan-mismatch` (memory) currently describe. This is a real design reversal, not a small tweak -- it likely means postsecondary content needs building for most of Lessons 2 and up (using the not-yet-used A/B letter-suffix convention from the rename above), not just Lessons 32-38. Not scoped or built yet; revisit before assuming the current Q1 lesson plans (which have zero postsecondary content) are complete.

**Next up:**
1. Finish the NTC Hosting DNS/production setup once the domain registration clears (items 5-6 above, still accurate) — likely less work than expected given the panel already has a Moodle skeleton provisioned.
2. Rebuild Lesson 2's checks/practice as interactive H5P (item 3 above still applies, now under the new naming).
3. Fill the missing-content gaps for Lessons 3/5/6/7 (item 7 above, now under the new naming).
4. Items 8-9 above are all still accurate and untouched.

## Python: Unit 01 brought to full consistency, 2026-08-30

Jay asked to start building out Game I (Python), picking Units 00 and 01. **Unit 00 turned out to already be done** (shared cross-course onboarding content, built 2026-08-18) — just needed `course-plan.md`'s stale pointer fixed. **Unit 01's "5 lessons not yet rebuilt" claim was also stale** — real gaps were much narrower: 3 missing interactive drills (categorization in 01.2/01.6, sequencing in 01.3), a missing `application.py` in 01.3, and (contested, see below) a missing project step in 01.1. Full detail in `decisions-log.md`.

**Bigger finding: ~475 em-dashes across the whole unit**, a real standing-rule violation nobody had caught. Fixed via a mix of forks and direct edits (two forks died mid-task to a session usage limit, finished by hand). Also changed the documented title-format convention (`N.N.N — Type` → `N.N.N Type`, no dash) to match, everywhere.

**Nested collapsible nav menu, which Jay asked about directly, turned out to already exist** (3-level `<details>` nesting) but is copy-pasted into all 47 HTML files with no shared source — the parallel lesson rebuilds caused real drift here (stale cross-lesson links, one lesson's copy dropped the other 5 entirely). Fixed with a new sync script, `02-authoring-system/tools/sync_unit01_nav_menu.py`; every link verified against the real filesystem afterward. This architecture is now documented in `mvp-unit-folder-structure.md` so future multi-lesson passes budget a sync step.

**Still open:** Jay hasn't confirmed whether 01.1 should keep its new pseudocode project step (reverses a deliberate 2026-08-20 "conceptual only, no project" decision the rebuild directive didn't know about) or revert to conceptual-only.

**Committed and pushed** (`77b3d64`) — everything below is already live, this session's work was verified programmatically (HTML balance, valid Python, no broken nav links, zero em-dashes outside dev comments) but not with a real rendered browser, since this droplet has no display and Playwright wasn't installed/available in the sessions that touched this. Needs a real click-through on Jay's desktop (`~/web-testing`'s existing Playwright harness, per `07-infrastructure/droplet-setup.md`) before treating any of it as fully proven:

## Needs Playwright/desktop verification (added 2026-08-30)

**New interactive content, never seen rendered:**
- `lesson_01_02_input_process_output/04_practice.html` — new Drill 9 (Categorization: sort 6 weather-app items into Input/Process/Output bins). The building fork explicitly flagged it couldn't get Playwright running and relied on syntax-check + manual trace only — weakest-verified piece of this whole pass. Check: drag-and-drop actually works, Check-button scoring is correct (a real bug was already caught and fixed here in the scoring logic, worth confirming the fix holds), telemetry logs attempts.
- `lesson_01_03_writing_your_first_program/05_practice.html` — new Drill 9 (Sequencing: reorder 4 scrambled `print()` lines via ▲▼ buttons, no native drag-and-drop). Check the reorder buttons work and final-order checking is correct.
- `lesson_01_06_common_syntax_mistakes/04_practice.html` — new Drill 9 (Categorization: sort broken lines into SyntaxError/NameError bins). Custom CSS was hand-built for this one (no existing styled demo to copy) — worth a visual check that it actually matches the lesson's established look.
- `lesson_01_01_what_programs_do/05_project.html` + `06_project.py` — brand new project step (design-a-program pseudocode task), only reviewed as text/HTML, never opened in a browser. Also gated on Jay's project-step decision above — hold this one until that's resolved.
- `lesson_01_03_writing_your_first_program/06_application.py` — new hands-on step, simple enough it's low-risk, but still unseen.

**Renumbered/restructured, check nothing broke in practice:**
- `lesson_01_01_what_programs_do/` — files renamed (mastery check 05→07, feedback 07→09), part-numbers and footer nav updated to match. Click through the whole lesson start to finish.
- `lesson_01_03_writing_your_first_program/` — files renamed 06→07 through 10→11 to make room for the new application.py. Same click-through check.

**The unit-wide nav menu itself** — verified programmatically (every link resolves to a real file) but never actually opened and clicked through. Check across a few different lesson pages: does the ☰ toggle open/close correctly, does each lesson's `<details>` expand/collapse independently, does the done-chip styling still render right, does the currently-open lesson default correctly per page.

**Lower priority, but worth a skim:** the ~475 em-dash fixes across 01.1-01.5 were mostly mechanical (period-split/colon/comma substitutions) — read through a page or two per lesson to confirm nothing reads awkwardly after the edit, especially in `05_practice.html`'s drill feedback strings (lesson_01_04) where several fixes landed inside JS template strings.

**Unit 01's content is now also viewable in Moodle** (`foxcs-python`, section 2), since Jay had no way to open local files from this droplet session before reaching his desktop. Overview page + all 6 lessons (each one Moodle resource, multi-file, with `00_table_of_contents.html` as the entry point) + the unit project pair, 9 resources total. **One real, permanent limitation**: the nav menu's links to *other* lessons don't work from inside Moodle (they'd silently show the wrong lesson's content, not a clean 404, so they were stripped from the Moodle copies specifically) — within-lesson navigation works fine, but moving between lessons in Moodle means going back to the section list. Full detail in `decisions-log.md`; scripts and reasoning in `07-infrastructure/moodle-scripts/python/README.md`. Access: same SSH tunnel as Seminar III (`ssh -L 8080:localhost:80 foxcs-droplet`, browse `http://localhost:8080`), admin login `FoxCS2026Admin!`.

**Moodle nav simplified per Jay's feedback** — the in-page menu on each Moodle-uploaded lesson now shows just that lesson's own files, not the whole "Unit 01 with 6 collapsed lessons" structure (redundant now that each lesson is its own Moodle tab). All 6 lessons re-uploaded with this (cmids 98-103).

**H5P Interactive Book piloted for Lesson 01.1** (cmid 97, right after the overview page, before the HTML-file version) — Jay asked whether this format might solve the navigation issues better than the file-based model. Covers Instruction/Flashcards/Vocab Quiz/Practice/Project with real adapted content; Mastery Check stays separate (no H5P equivalent for the password-gate mechanism), matching the original Two-Surface model. **Hit a genuinely nasty silent bug** building this — H5P's `InteractiveBook` chapters need to be bare `Column` objects, not wrapped in `{"chapter": {...}}` like the semantics field name suggests; the wrong version uploads with zero errors but Moodle quietly strips all chapter content. Found by reading Moodle's own validator source, documented in `h5p-content-type-guide.md` so it's not rediscovered. Full detail in `decisions-log.md`.

**Jay's subitem-click "blank page" report is still not independently diagnosed** — checked Moodle's embed iframe code for sandbox restrictions (none found), but couldn't fully test without a real browser. Becomes moot for 01.1 if the Interactive Book pilot gets adopted.

**Interactive Book pilot: title/labeling pass, 2026-08-30 (same day).** Jay's real feedback after reviewing: every chapter showed as "Column" (H5P's own generic class-name fallback), every question showed "Untitled Multiple Choice", and a position counter read "Column 6/7" instead of something meaningful -- all traced to one root cause: every block in the pilot script had a copy-pasted generic placeholder title instead of a real one. Fixed by threading a required `title` parameter through every `block_*()` helper and `chapter()`, giving all 6 chapters and all 15 sub-content pieces (2 quick-checks, 4 vocab questions, 7 practice questions, 1 flashcard deck, 1 essay) real descriptive titles adapted from their actual content. Verified directly against the DB-stored `jsoncontent` (not the escaped embed-page blob, which gave false negatives on a naive grep check) -- confirmed zero generic titles remain anywhere in the tree. Re-uploaded as cmid 104.

**Jay also reported the book's top content gets cut off when clicking a nav item.** Checked what's checkable without a browser (no iframe sandbox restriction found, same as the earlier subitem-click report) -- likely an iframe-resize-vs-scroll-position interaction (H5P's content height changes on internal chapter navigation, but the parent Moodle page doesn't necessarily re-scroll to keep the new content's top in view), a known class of issue with resizable iframes, not something traceable to the content itself. Not fixed, needs the Playwright pass to actually see it happen.

**Interactive Book pilot: second round of polish, 2026-08-30 (same day, later).** More real feedback from Jay after the title fix:
- Position counter ("6/7") duplicated each chapter's own on-page title, confusing since the same word appeared twice. Set the `page` content field (a top-level l10n string in InteractiveBook's semantics, defaults to "Page") to "Learning Activities" -- a distinct, generic counter label instead of the chapter's own name repeated back.
- Project chapter asked for an "if this, then that" instruction without ever teaching the pattern. Added a real "How to Write an 'If This, Then That' Instruction" section (explains the if/then shape, points back to the vending-machine example already in Chapter 1, gives one new worked example using an alarm clock) right before the task.
- Project's requirements were one flowing paragraph. Rewrote as a real `<ul><li>` bulleted list.
- Practice needed "sufficient" prep -- was 7 questions (one original drill, the multi-select "check all that apply," was dropped during the port rather than converted). Added 2 new non-redundant questions (Game Connection reasoning, applying the recipe/program comparison) for 9 total, deliberately not just re-adding a redundant question given Practice already had a book-themed one covering similar ground.
Re-uploaded as cmid 105. Verified via DB: counter label set, all 4 fixes' content present, question count correct.

**Real root cause found for both nav complaints (cut-off-top-on-click, lose-the-menu-on-scroll) -- likely the same bug.** Pulled `H5P.InteractiveBook`'s actual CSS straight from Moodle's file storage (`mdl_files`, `core_h5p`/`libraries` component): the library is built to run in a *fixed-height* container with its own internal `overflow-y:auto` scroll, and its nav header uses `position:sticky` relative to that internal scroll. Moodle's H5P embed instead auto-resizes the iframe to fit all content (no internal scroll ever triggers), which breaks `position:sticky` since it can't reach across an iframe boundary to stick against the *parent* document's scroll -- the whole Moodle page scrolls instead, and the nav just scrolls away with everything else. Confirmed `H5P.InteractiveBook` is flagged fullscreen-capable in `mdl_h5p_libraries` (`fullscreen=1`) -- fullscreen mode gives it a real bounded viewport, which should fix both symptoms without any code change. Worth trying first, next time Jay's actually in the book. A deeper fix (forcing Moodle to embed H5P activities in a fixed-height frame instead of auto-resizing) is possible but is a site-wide infrastructure change affecting every H5P activity, not scoped to this pilot -- not attempted without real-browser confirmation first.

**Interactive Book pilot: third round, 2026-08-30 (same day, later still) — content completeness + concept-teaching quality.** Jay's feedback:
- The if/then pattern needed to be taught more meaningfully, not just stated in one line, and "if"/"then" needed bolding throughout to signal importance. Rebuilt as a real 2-part breakdown (labeled IF/THEN parts, a worked example already seen in the lesson, then a genuinely new one using an alarm clock), with every occurrence of if/then bolded, including retroactively in Chapter 1's original vending-machine quote.
- **Mastery Check and Feedback needed to actually be in the book**, not left as external "go do this elsewhere" references. Added both as real chapters (6 and 7), adapting the exact same questions from the real `07_mastery_check.html` (4 essay questions) and `09_feedback.html` (5 essay questions, folding the 1-5 rating scales into the essay prompt text since no H5P widget here does a clean numeric rating). **Flagged, not silently dropped:** the real mastery check is password-gated by the teacher with auto-timestamps; H5P has no equivalent for either, so this book version has neither. Real, known gap in this format — Jay's call whether that matters enough to block adoption.
- Caught and fixed a real self-inflicted bug while writing this round's content: used `&mdash;` (an em dash entity) in the new IF/THEN breakdown, violating the standing no-em-dash rule. Full sweep of the whole build script confirmed clean before re-upload.

Now 7 chapters, 10 Essay questions, 15 MultiChoice questions, 1 Dialogcards deck, zero validity errors, zero em-dashes. Re-uploaded as cmid 107.

**Interactive Book pilot: simplified individual question titles, 2026-08-30 (same day, yet later).** Jay's feedback: descriptive per-question titles ("Quick Check: What Is a Program?", "Vocab: program", "Practice: The Programmer", etc.) were more than needed -- simpler to just call them "Question 1", "Question 2" within each chapter. Applied everywhere individual question-type items appear (2 Quick Checks in Instruction, 4 Vocab Quiz questions, 9 Practice questions, 4 Mastery Check questions, 5 Feedback questions) -- chapter-level titles (Instruction, Flashcards, Vocab Quiz, etc.) and the two singular items (the Flashcards deck, the Project essay) kept their real names, since "Question 1" only reads as simpler when there's more than one in the same list. Re-uploaded as cmid 108, verified via DB.

**Real submission-mechanism gap surfaced, and a big potential direction change, 2026-08-30 (same day, final).** Jay asked about password-protected mastery checks, which led to realizing today's whole Moodle upload effort (file-based lessons + Interactive Book) is a **read-only preview mirror** -- the real submission path is still Google Classroom (students submit the whole folder back through Classroom, Jay grades from the download). Jay said he'd be open to making **Moodle the real replacement for Classroom** -- a large undecided migration (enrollment, per-content-type submission mechanism, `05-grader/` rework), deliberately not scoped in full. Started narrow instead: **built Lesson 01.1's Mastery Check as a real native Moodle Quiz** (4 essay questions, real "require password" setting -- Moodle's own supported mechanism, not hand-rolled) since that's the one piece H5P categorically can't do at all. Hit and solved two real API gotchas along the way (Moodle's PHPUnit-only test generators aren't usable standalone; `quiz_update_sumgrades()` was renamed to a class method) -- full detail with the actual fixes in `decisions-log.md` and the script's own header comment. Verified end to end: fetched the real quiz page and confirmed an actual password input renders, tied to Moodle's native access control. Live as cmid 114, right after the Interactive Book pilot -- a standalone proof, not yet wired into either lesson version as "the" real mastery check.

**Next up (Python):**
1. **Jay's decision on the Moodle-replaces-Classroom question** — how far to take it, now that Mastery Check has a working proof-of-concept. This is the biggest open fork right now, bigger than the Interactive Book question below.
2. **Jay's review of the now-complete Interactive Book pilot vs. the HTML-file version**, including trying the fullscreen button for the nav-loss issue — decides whether to port Lessons 01.2-01.6 to the same format.
3. Get Jay's call on 01.1's project step (HTML-file version).
4. The Playwright verification checklist above, once Jay's on his desktop — now also covers reviewing the Moodle copies (both versions), the subitem-click symptom, whether fullscreen mode fixes the sticky-nav problem, and clicking through the real password-gated Quiz.
5. Em-dash sweep beyond Unit 01 — Unit 00's shared onboarding content and the rest of FoxCS haven't been checked.
6. Units 00 (now resolved) and 02-20 are still the real remaining Python scope — only Unit 01 has content, in the repo or in Moodle.

## 2026-08-31 — Pending: XP tracking for Game II/Web Dev/Game of the Week's H5P content

**Not started, deliberately deferred per Jay.** He asked to make sure Game II, Web Dev, and Game of the Week content acknowledges how much XP each activity is worth, with the grader tracking and totaling XP earned per activity/lesson per student. Real, locked-in XP point values already exist (`02-authoring-system/xp-and-incentives.md`, locked 2026-08-22) and a real grader exists (`05-grader/school-side/auto_grade.py`), but both were built specifically for Game I/Python's older content format: self-saving HTML pages carrying a hidden `foxcs-telemetry` JSON block or timestamp spans, which `auto_grade.py` parses directly from the file.

**Real architecture gap, not just missing labels:** all of tonight's new Game II/Web Dev/Game of the Week content (the onboarding Interactive Books, both pathway-placement quizzes, the Choose Your Pathway selections, Game of the Week's Lesson 1 book) is H5P-based, deployed through Moodle. H5P activities already record scores, attempts, and completion natively in Moodle's own database (`mdl_h5p_*` tables, gradebook), a completely different mechanism from the file-based telemetry-blob pattern `auto_grade.py` currently reads. Retrofitting H5P content to also carry a redundant telemetry blob would duplicate data Moodle already has; the more natural fix is a new, separate script that reads XP-relevant data straight from Moodle's database and produces a per-student total, but that's real, undesigned software, not a quick add-on.

**Not yet mapped:** which of tonight's specific activities correspond to which existing XP category (Vocab Quiz = 5, Journal Entry = scales with word count, Mastery Check = 20, etc.) — some, like the pathway-placement personality quizzes and the Choose Your Pathway self-report question, don't cleanly match any existing category and would need a real decision, not an assumption.

**Next steps, whenever this gets picked up:** (1) decide whether Moodle-native XP tracking reads from `mdl_h5p_xapi_results`/the gradebook directly, or some other mechanism; (2) map each tonight-built activity to an XP category or decide it earns none (matching the existing "no XP for passive content" rule); (3) design the aggregation/lookup step fresh for this data source rather than assuming `auto_grade.py`'s existing pipeline extends cleanly.

## 2026-08-31 (later same day) — Real student data loss on Game of the Week Lesson 1, and a process failure to fix

**What happened:** while continuing tonight's build session, `foxcs-gotw`'s "Lesson 1: Telephone" H5P activity was rebuilt and redeployed three times in a row (once to split the reflection into a server-saved format, once to add em-dash fixes and concrete game examples, once to add the Overcooked image) using `delete-modules.php` to remove each superseded version. Partway through, it came to light that **school was already in session and real students were actively working in Moodle** during this exact window — confirmed directly via `mdl_h5pactivity_attempts`, real attempt rows from real codenamed accounts (Game I 8th period, Seminar III 5th period) timestamped 1-3 hours before the discovery, on other activities (Game I's onboarding book, Seminar III's Choose Your Pathway).

**The damage:** `course_delete_module()` fully purges attempt/gradebook data when a module is deleted, and none of the three GotW rebuilds checked for existing attempts before deleting the prior version. The current live version has zero attempts and there is no orphaned data trail to check retroactively, so **it cannot be confirmed or ruled out whether a real student had already submitted a Lesson 1 reflection before it was deleted out from under them.** Game I's onboarding book and Seminar III's Choose Your Pathway were not rebuilt after real usage began, so their data is confirmed intact and was never at risk.

**Jay's resolution, 2026-08-31:** since the data may be genuinely lost and can't be recovered, **every student should automatically get full credit for Game of the Week Lesson 1 (Telephone)**, regardless of whether their actual submission survived. Don't ask students to prove they did it or resubmit; treat it as done for everyone. Needs to be reflected wherever GotW Lesson 1 grades/completion get recorded once that pipeline exists (see the XP-tracking entry above) — flag this activity specifically as auto-credited, not graded normally.

**Standing process rule going forward, per Jay:** if a session picks back up after being interrupted, or otherwise isn't continuous, pause before deleting or overwriting anything live and check whether real activity has happened in the gap (attempt counts, timestamps) rather than assuming the state left at the end of the last turn is still current. This applies generally, not just to Game of the Week.

## 2026-08-31 (later still) — GMetrix Unity courseware structure, real gap on VR Developer; Jay's Unity pathway-structure thinking (deferred, revisit after first few lessons are built)

**Context:** While researching real cert objectives (see above), Jay asked for a quick look at what's actually inside GMetrix's 3 Unity courses (Programmer, Digital Artist, VR Developer Next Generation) — not full objective-by-objective depth, just enough to know what's there. Explicitly deferred going deeper until the first few lessons across the 4 pathways are built, since students won't reach real Unity work immediately anyway (MakeCode first, then 2D, then 3D).

**What's actually in each course, confirmed live in Jay's GMetrix account:**
- **Programmer**: real LearnKey video courseware, 12 modules — Introduction to Programming and the Unity Engine, Create Your First Script and Add Instructions to the Game, Flow Control/Editing Tools/Naming Conventions, Introduction to Character Movements, Introduction to Cameras in Unity, Introduction to Art Pipeline in Unity, Constrain the Character Jump, Introduction to Unity UI Elements, Create Trigger Events, Visual and Audio Effects, Introduction to Timeline and Cinemachine, **Introduction to ECS**. C#-based throughout.
- **Digital Artist**: no video modules at all — instead 4 real courseware **projects**: Architecture Project Part 1, Architecture Project Part 2, Level Design Project, Real-Time Graphics Project. Different content shape entirely from Programmer, project-driven rather than lesson-driven.
- **VR Developer (Next Generation)**: **no courseware/video content of any kind** — only 2 practice exams and the study guide. Real, genuine gap compared to the other two tracks: there's nothing here to scaffold a student through VR concepts the way LearnKey's videos or Artist's guided projects do. If Jay wants VR Developer offered as a real specialization, either real FoxCS-authored instructional content or an outside source (Unity Learn, most likely) has to fill that gap entirely — GMetrix provides nothing to lean on for this one.

**Jay's current thinking on Unity pathway structure (his own words, lightly organized, not yet decided or built):**
- Students get a choice among **3 Unity cert prep options**: Programmer, Digital Artist, VR Developer. Possible shape: two combined tracks, **Programmer → Artist** and **Programmer → VR Developer**, both starting from the same Programmer foundation (matches the already-confirmed "Programmer is required, Artist is achievable" framing in `game-programming-2/CLAUDE.md` — VR Developer would be a new third option added alongside Artist as the second-stage choice, not a replacement for either).
- Alternative, simpler shape Jay also floated: **one shared Unity course**, with students choosing which path(s) to pursue within it, rather than fully separate course shells per specialization.
- **Real skill-progression goal, independent of which structure gets picked:** MakeCode Arcade first (already the established Game I post-cert direction, reused here as the on-ramp), then 2D Unity, then 3D Unity — with **Unity Learn** (learn.unity.com, Unity's own free tutorial platform, licensing not yet checked per `game-programming-2/CLAUDE.md`'s existing open question) used for the initial skill-building phase before shifting into cert-track-specific content (Programmer videos / Artist projects / whatever fills the VR gap). Jay's own framing: this is about making sure students "feel they can build their skills so they are not lost once they get to the Unity platform itself and the training wheels are off a bit" — Unity Learn and MakeCode are the training wheels, cert-track content is where the wheels come off.
- **Explicitly not being scoped in full detail yet** — Jay wants the first few Lesson 1s (Game I, Web Dev, Software Dev, Game Design/Unity) built first, then come back to design this properly.

**Resolved separately, same conversation:** the Software Development exam's C#-vs-Java discrepancy the research fork flagged (see above) is settled — per Jay, **Java is the only OOP language available in the course, and it satisfies the exam's accepted-alternatives list** (C#, Java, or C++ all count). No need to add C# or change the Java-based Stage 4 plan.

**Workbook pages:** Jay is downloading additional official workbook pages into `starter context/` himself — has already added Java, Software Dev, and HTML & CSS workbook pages as of this entry. Check `starter context/` fresh before assuming what source material exists, rather than trusting this list once more get added.

## 2026-08-31 (later still) — Jay scopes real, permitted student AI use, likely activating the already-planned "Future Direction" phase in `academic-integrity-ai-use.md`

**Context:** Prompted directly by the research fork's finding that the Java and Software Development Certiport exams both list using generative AI as a research/debugging tool as real, testable exam content (see above) — Jay clarified his actual intent, unprompted, rather than leaving the tension unresolved.

**Jay's statement, close to verbatim:** he wants students using Generative AI in their courses, but "needs to know that they use it properly." Explicitly okay to build in real course content about how to use AI to help study. Wants students actually using AI to help build things like web pages, modeling FoxCS's pathways after real-world professional skills (working developers use AI tools). **The hard line: no AI on the actual certification exam** — so students need to build the underlying skills that are exam-relevant and be able to do the important parts of the work on their own, independent of AI, when it counts. Also explicitly named: students need to be able to **analyze and interpret code**, not just generate it.

**This is very likely Jay activating (at least partially) the phase `01-privacy-and-governance/academic-integrity-ai-use.md` already anticipated and deliberately left inactive**, quoting that doc directly: *"Jay will explicitly state, in advance, exactly when AI use is allowed and in what capacity... a more permissive documented-use model (used specifically to help students break down problems, check their own work, or walk through what they didn't understand) is the actual intended direction."* Today's statement reads as exactly that kind of explicit scoping the doc was waiting for — but it isn't identical to the doc's original framing (that one was about breaking down problems/checking work/walking through confusion; today's adds a genuinely new dimension, using AI as a real *building* tool for practice work, modeled on professional practice, not just a study aid).

**Not yet done, and shouldn't be inferred/built silently:** `academic-integrity-ai-use.md` itself has NOT been edited to reflect this — that's a carefully-built policy doc with real, serious consequences attached (0%/F, a call home, a permanent Aspen record for violations), and this session flagged rather than silently rewrote it, pending Jay confirming the exact scope in writing (which specific assignment types/contexts get the new permission, whether "no AI on the exam" extends to FoxCS's own internal mastery checks too or just the real external certification exam, and whether the existing all-or-nothing default still applies to anything not explicitly carved out). Real next step: get that scope nailed down with Jay directly, then update the policy doc for real, rather than guessing at the boundary.

**Practical curriculum note to carry into Lesson 1 authoring:** "students need to be able to analyze and interpret code, not just generate it" should show up as a real, explicit skill emphasis in the lessons being built next — code-reading/debugging/interpretation exercises alongside any AI-assisted building work, not just "let AI write it and move on."

**Real scope detail added immediately after, same conversation:** per Jay, **the Level 2 pathways (Web Dev, Software Dev, Game Design/Unity) can lean heavier into AI use than Game I/Python (Level 1)**. This is a genuine, concrete split worth building into the policy update once it happens — not a uniform platform-wide permission. Still not enough on its own to fully update `academic-integrity-ai-use.md` (exact assignment-type boundaries within Level 2 still undefined), but narrows the real next question considerably: Level 1 (Game I) likely stays closer to the existing all-or-nothing default, Level 2 gets the more permissive, AI-as-a-real-building-tool treatment.

**Specific sequencing rule, added same conversation, for the HTML5 App Dev major project specifically:** students may use AI to build the *skeleton* of their program, but only **after** they already understand how the underlying code works — AI-assisted skeleton-building comes after real comprehension, not instead of it or before it. Don't design the major project's early steps around "ask AI to generate it," even though AI-assisted building is the eventual expected workflow for that project — sequence real understanding first.
## 2026-08-31 (evening) — Python 01.1 finalized live: tabbed rebuild, completion tracking, due date, Mastery Check integrity gate, sitewide timezone fix

**Instruction rebuilt as tabbed native Lesson content (cmid=193), replacing the 7-page linear pilot** — one tabbed Content page (5 tabs, pure CSS/no JS, real click-to-flip flashcards for Key Terms) → Quick Check A → Quick Check B → a new Vocab Quiz using native `LESSON_PAGE_MATCHING` with real branching (wrong → Vocab Breakdown reteach → Vocab Retry using scenario-phrased, not verbatim-repeated, definitions, terminal either way). Verified end-to-end live via browser: tabs/flashcards render with zero CSS collision against Moodle Boost's Bootstrap classes (used `foxcs-` prefixed classnames specifically to avoid this), both Quick Checks and both Matching pages score/route correctly, clean end-of-lesson, zero console errors. Per-tab "click to reveal" checkpoints were built, tested, then **cut per Jay's direct call** — they don't make a student do anything provable, same reasoning that's now logged in `02-authoring-system/objectives-and-skills-proficiency.md`'s new "Mechanism Choice" section.

**Labeling norm, confirmed final:** Instruction gets no suffix at all — just `01.1 What Programs Do` (the lesson's own title). Everything else gets a short, undecorated label: `01.1 Practice`, `01.1 Mastery Check`. No module is named with a parenthetical role suffix anymore.

**Cleanup:** deleted cmid=98 (old MVP static "01.1" resource) and cmid=117 (old H5P Instruction book) — both verified hidden with zero real attempts before deletion, both fully superseded by the tabbed rebuild. Did **not** touch cmid=91 (Unit 01 Overview) or 99-103 (01.2-01.6 MVP placeholders) since those aren't superseded by anything real yet. Section sequence reordered so real 01.1 content displays in completion order: Instruction → Practice → Project materials → Mastery Check, with the still-needed future placeholders after.

**Completion tracking turned on** (was off sitewide on all three 01.1 modules before this): `completion=2` + `completionendreached=1` on Instruction and Practice (reaching the end requires answering every real Question/Matching page along the way, since nothing skips), plus Instruction's existing `completionpassgrade=1` / 50% grade-item threshold as an additional real-performance signal. XP eligibility should key off these native signals, not off anything inside the ungraded tab/flashcard content.

**Due date set:** 2026-09-01 15:30 America/Chicago (unix 1788294600) on Instruction's and Practice's `lesson.deadline`, and Mastery Check's `quiz.timeclose` — applied consistently across all three per Jay's explicit "the due date for 01.1 is 9/1 at 3:30 PM," with the password-gate system (below) acting as a readiness gate *within* that window, not a replacement for the hard deadline. 01.2 and 01.3 due dates intentionally not set yet — Jay wants to come back to those later in the week.

**Revisit capability confirmed already in place:** both lessons already had `retake=0` + `modattempts=1` from their original builds, which gives real Moodle review mode (walk back through the same pages read-only, own answers + feedback shown) — this already satisfies "students should be able to revisit content if they want to," no new setting needed.

**Real sitewide bug found and fixed:** Moodle's default timezone was set to **Europe/London**, not America/Chicago — discovered because the 3:30 PM due date displayed as 9:30 PM (a 6-hour BST-vs-CDT offset) for the admin account, which uses server-default timezone like most/new student accounts would. The stored UTC value was always correct; only the display was wrong, sitewide, for every date/timestamp on the instance, not just this one due date. Fixed with Jay's explicit confirmation (`Site administration → Server → Location → Default timezone` → America/Chicago), verified live afterward (due date now correctly shows 3:30 PM).

**Mastery Check (cmid=114) made visible** (was hidden) and got a real pre-attempt warning written into its intro text: this is a scored attempt not practice, don't start until Practice/Project work is done, ask the teacher for the password when ready. **A new required first question was added** (shifting the 4 existing real essay questions to slots 2-5): a short-answer academic-integrity acknowledgment, `maxmark=0` (doesn't dilute the 4 essays' average grade), with the exact required consequences language Jay dictated — no unauthorized sources, no study materials, no sharing with/receiving from peers, no phone in possession, or a 0%/F and ineligibility for retakes. **Per Jay's own instruction, this specific question must always be first on every future Mastery Check, not just 01.1's** — worth remembering as the pattern gets replicated to 01.2+ and other pathways.

**Password vs. Safe Exam Browser — real open question, not yet resolved.** Jay asked directly whether per-student passwords are worth keeping vs. just locking down the browser. Verified `quizaccess_seb` (Safe Exam Browser) is actually installed on this instance, not just theoretically available. My read, given to Jay but not yet acted on: these solve different problems, not the same one — a password (even a real, single, currently-shared one, `T4WPR8`) is a *readiness gate* (controls **when** a student starts, teacher-approved), while SEB is an *integrity control* (restricts **what** a student can do once inside the attempt — outside tabs/apps/copy-paste). Neither, and no software at all, stops a student from having a phone in their pocket; that stays a physical/procedural rule enforced in the room regardless. Recommendation given: don't choose one or the other — keep the password as the readiness gate, layer SEB on as the actual technical integrity control. Not implemented yet, no per-student password mechanism built.

**Roster spreadsheet per-student Mastery Check password column — scoped, not built.** Jay wants the roster (`06-data-and-spreadsheets/`, itself still a placeholder per `CLAUDE.md`) to support assigning/emailing an individual password per student for Mastery Check access, plus flagged, explicitly as long-term/future thinking (his own framing), a self-service "student requests a password when ready" flow. Neither built this session — flagged here as a real open item since `06-data-and-spreadsheets/` doesn't exist yet as real infrastructure to extend.

**Structural clarification from Jay, narrows earlier assumptions:** Project is scoped **per unit, not per sub-lesson** — one Project for all of Unit 01 (already correct: `Unit 01 Project: Interactive Greeting` + starter file, cmid 92/93, is the one Project for the whole unit, not 01.1-specific) is the intended pattern, at least for now ("perhaps there are more projects in some units" — not ruled out, just not the default). **Each unit should also get a Reflection**, as its own recurring element alongside the one Project — not yet built for Unit 01; a real open item, distinct from the existing per-sub-lesson Quick Checks/Vocab Quiz/Mastery Check pattern.

**Real architecture finding, verified not assumed: native Lesson page content renders with `noclean => true`** (confirmed directly in `mod/lesson/locallib.php`'s `get_contents()`) — meaning `<script>`-driven interactivity, like the old MVP-era `01_instruction.html` files (courses/python/content/.../lesson_01_02_.../), genuinely would survive and execute inside a native Lesson page. Recommended against reusing those files wholesale anyway, for two concrete reasons found by actually reading `lesson_01_02_input_process_output/01_instruction.html`: (1) their quick-checks are pure client-side JS with zero Moodle-side tracking, same unprovable-engagement problem the click-to-reveal checkpoints just got cut for; (2) they still carry the old cross-lesson unit menu (links to every step of 01.1-01.6 from any page), which Jay explicitly moved away from earlier this session. Recommendation: port the actual instructional content/diagrams (genuinely good, not wasted) into the same tabbed-native-Lesson pattern as 01.1, rebuild only the quick-checks as real native Question pages. Not yet done for 01.2-01.6 — next real content task once Jay confirms direction.

## 2026-09-03 — Authoring-checker test coverage, browser-verified save-flow check, and Lesson 01.4 build started (Instruction live, Practice scripted)

**Local repo was 11 commits behind `origin/main`**, discovered mid-session via `git status` after finding untracked build/finalize scripts on disk that didn't match this file's own narrative — those turned out to be real, already-pushed work (01.1's tabbed-rebuild finalization) that just hadn't been pulled to this machine yet. Stashed all local uncommitted work (including several genuinely new untracked files), fast-forwarded, resolved one merge conflict in this file (upstream's side was strictly newer/complete, kept it), restored the stash, and verified every "already exists, no checkout" file was byte-identical to what the pull brought in before dropping the stash. **Standing lesson reinforced, not new:** this repo's own "pause and check for real activity in the gap" rule (2026-08-31 entry above) applies to git divergence too, not just live Moodle state.

**`foxcstest` couldn't see `foxcs-python` — diagnosed, not a real permissions bug.** Checked directly against the DB: enrollment, role assignment, and course/section visibility were all already correct (`is_enrolled: YES`, `can_access_course: YES`). Most likely cause: a stale Moodle cache from tonight's direct-DB build scripts not calling Moodle's own cache-rebuild helpers. Ran `admin/cli/purge_caches.php` as the fix — standard, safe, non-destructive.

**Confirmed ground-truth, not assumed:** queried `mdl_course_modules` directly and verified Lessons 01.1, 01.2, and 01.3 all really do have live Instruction/Practice/Mastery Check modules (01.3 additionally has its own "01.3 Coding Exercise" `assign`), matching what Jay reported from memory.

**Built real test coverage for the four content-authoring "gotcha" checkers in `02-authoring-system/tools/`** (none had tests before tonight, despite three of them existing since 2026-08-31): `check_save_serialization.py`, `check_shuffle_persistence.py`, and `check_eliminable_distractors.py` each got a full `unittest` suite (34 tests total) in a new `02-authoring-system/tools/tests/`. `check-lesson-ladder-wiring.php` got a plain-PHP test harness (29 assertions, no PHPUnit dependency — matches this repo's stdlib-only testing convention) since its own header comment described a harness that was never actually written. Two new checkers were added and tested the same way: `check_mastery_check_completeness.py` (verifies every lesson's teacher-materials answer key actually exists — directly closes `authoring-flow-gaps-2026-08-11.md`'s gap #9, which was never actually checked before tonight; confirmed all 6 real Unit 01 KEY files exist and are well-formed) and `check_drill_feedback_completeness.py` (every practice drill needs real correct-and-incorrect feedback text, not just a pass/fail toggle — found one legitimate false positive, a "sort into categories" progress counter in 01.6's practice file, correctly explained by the checker's own documented limitations). All wired into a new `.github/workflows/authoring-checks-tests.yml`.

**Real-browser (not just static-pattern) verification of the save-serialization fix**, since the Claude-in-Chrome extension wasn't connected in this session: `browser_verify_vocab_quiz_save.mjs` uses Playwright/Chromium (added as a real devDependency, `02-authoring-system/tools/package.json`) to actually drive Lesson 01.4's real vocab quiz like a student — match all 5 terms, type a reflection, click Save — then reads the downloaded file back and confirms the typed text survived. All 5 checks passed against the real file. Wired into CI as a third job in the same workflow (needs Chromium install, no Moodle instance required).

**Added a "Block Builder with Distractor Pieces" component** to `02-authoring-system/component-library/index.html` (#2b), per Jay's request for Duolingo-style block-building drills with extra decoy pieces in the bank. Confirmed with Jay: still a single correct/incorrect drill with one anticipated right answer even with distractors present — no new grading logic needed, just a bigger bank (target pieces + genuine near-miss distractors tied to a real misconception, same standard as the existing multiple-choice distractor rule). Verified working in a real headless browser (5/5 checks: correct build passes, using a distractor fails, reset restores the full bank, unused distractors stay in the bank).

**Lesson 01.4 (Printing Output) build started, per Jay's "get 1.4 up for tomorrow" request.** Full plan in `07-infrastructure/moodle-scripts/lesson-01-04-build-plan.md` — read that before continuing this lesson's build, don't re-derive from this entry.

- **Instruction is live**: cmid=212, lessonid=11. Built via `build-lesson-01-04-instruction-tabbed.php` (4-tab content ported from the real `01_instruction.html` prose: print() Basics / Two Common Mistakes / Game Connection / Key Terms w/ 5 flip-card flashcards) + 2 new Quick Checks (deliberately new broken-print-statement variants, not restating the tabs' own two worked examples) + a 5-term Vocab Quiz (native Matching, wrong → Breakdown → Retry with rephrased definitions). Verified directly against the DB: `maxanswers=6` (not 4 like 01.2's script — 5 terms here, a real setting difference flagged in the build plan so it isn't missed copying this script forward again), `custom=1`, `retake=0`, all 6 pages present with correct answer counts.
- **Practice is scripted but NOT yet run.** `build-lesson-01-04-practice-ladder.php` — syntax-checked, not yet executed against the live DB. **Redesigned once already tonight**: the first draft grouped clusters by which of `05_practice.html`'s 8 drills they came from; checking that against 01.4's actual stated objectives in `01_instruction.html` (per Jay's direct "make sure content matches the objectives" instruction) showed the drill-source grouping didn't map cleanly onto them. Rebuilt as three clusters, one per real objective: 01.4a predicts_print_output, 01.4b diagnoses_print_syntax_errors, 01.4c identifies_print_statement_parts (the language objective). Every broken-statement example across all three Practice clusters is a checked-not-assumed distinct variant from the ones already used in Instruction's Quick Checks and the Mastery Check.
- **Mastery Check and the new "01.4 Coding Exercise" Assignment are spec'd but not built** — real content mapped in from `09_mastery_check.html`/the teacher KEY and `07_project.html`/`08_project.py` respectively, exact verbatim required-first academic-integrity question text included, the `quiz_update_sumgrades()` rename gotcha called out. See the build plan doc for both in full.
- **Real decision made tonight, not deferred:** 01.4 gets its own per-lesson `01.4 Coding Exercise` Assignment, resolving the per-lesson-vs-per-unit inconsistency the build plan had flagged — 01.3's real precedent (its own per-lesson `assign`) and 01.4's own content (a complete standalone project, not a Unit-project pointer) both point the same direction.
- **Stopped here for the night per Jay's direction** ("push your changes... so I can start there tomorrow") rather than continuing straight through to a fully live 01.4 — Practice/Mastery Check/Coding Exercise are the real remaining work, all spec'd in detail in the build plan doc, not just flagged as TODO.

## 2026-09-03 (continued) — Lesson 01.4 build completed: Practice, Mastery Check, Coding Exercise, and a new Feedback activity

**Full detail in `decisions-log.md`'s matching entry — read that first, this is the short version.** Before touching anything, verified live state hadn't drifted since last session (repo up to date with origin, cmid=212 still 6 pages/zero attempts) per this file's own standing "don't assume the gap was quiet" rule.

- **Practice** (cmid=213): ran the already-staged `build-lesson-01-04-practice-ladder.php`, `check-lesson-ladder-wiring.php --cmid=213 --pool-cap=2` passed clean (0/0/0).
- **Mastery Check** (cmid=214, hidden): build plan's "4 fixed essays" spec was stale — verified against `mdl_quiz_slots` that 01.1/01.3 actually use a password + random-draw-of-1-from-10-pool design since a 2026-09-01 redesign the plan doc never picked up. Confirmed with Jay this is the real standard; built 01.4 to match (`Lesson 01.4 Mastery Check - Task Pool`, 10 tasks: 4 ported + 6 new, 2 of the 6 filling a real gap — none of the original 4 tested the function/string/argument language objective). SEB skipped, due date deferred, both Jay's explicit calls.
- **Coding Exercise** (cmid=215, hidden): mirrors cmid=206 (01.3) exactly — simple point grading, not a rubric; onlinetext + file submission; no feedback plugins enabled (matches 01.3's real config).
- **New, prompted by Jay mid-session: Feedback/reflection** (cmid=218, hidden) — the first `mod_feedback` activity built for any lesson in this course. Ported `11_feedback.html`'s already-authored 6-question reflection (never built in Moodle for 01.1-01.3 either — real standing gap). Deliberately NOT folded into the Mastery Check pool: a pool question only reaches ~1 in 10 students per attempt, wrong mechanism for a reflection meant for everyone. Built via Moodle's own `feedback_get_item_class()->set_data()->save_item()` API, 8 items, all read back and verified against the DB.
- Reordered Unit 01's section sequence: 212(Instruction)→213(Practice)→215(Coding Exercise)→214(Mastery Check)→218(Feedback), matching 01.1's completion-order convention. Left cmid=101 (01.4's old hidden MVP placeholder) alone — zero real student activity on it, but deletion wasn't asked for.
- Caches purged after every direct-DB write, per this doc's own checklist.

**Next up:**
1. **Jay's review of all 3 hidden 01.4 modules** (Mastery Check, Coding Exercise, Feedback) before flipping them visible — same gate every prior lesson's pieces went through.
2. **Port 01.1/01.2/01.3's own `NN_feedback.html` into `mod_feedback` too**, now that 01.4 proved the pattern — a real, not-yet-done follow-up, not just a nice-to-have.
3. Click-through verification as `foxcstest` (tabs/routing/password gate/checkbox behavior) — not done this session, same gap the build plan's own checklist already flagged.
4. 01.2's own Mastery Check/Coding Exercise status wasn't re-verified this session (only 01.1/01.3 were checked directly against `mdl_quiz_slots`) — worth confirming it matches the pool pattern too before treating "all of 01.1-01.4 are consistent" as settled.

## 2026-09-03 (continued again) — Real-browser submission verification for all 3 new 01.4 activities

**Jay asked to confirm submissions actually save and won't cause issues for students.** Did this for real, not just structurally: temporarily flipped cmid 214/215/218 visible, logged into the live site as `foxcstest` (real Student-role account) with Playwright/Chromium, and drove each activity's real submission flow end to end.

- **foxcstest's stored password didn't match `create-foxcs-test-student.php`'s documented value** (`FoxcsTest2026!`) -- confirmed via `authenticate_user_login()` directly, unrelated to any browser issue. Reset it back to the documented value (safe: dedicated test account, not a real student) so the script and the doc agree again.
- **Mastery Check**: real gotcha found -- Moodle's quiz password field uses a "click to enter text" sensitive-field toggle (a decoy box hides the real `<input class="d-none">` until clicked, presumably to block password-manager autofill). `page.fill()` alone times out against a `d-none` element; the toggle has to be clicked first. Completed a full attempt (integrity + the randomly-drawn task-pool question), submitted, and read the exact typed text back out of `mdl_question_attempt_step_data` -- confirmed it survives, byte for byte, tied to a `state=finished` attempt.
- **Coding Exercise**: submitted via the onlinetext editor, confirmed "Submitted for grading" in the UI and the exact text in `mdl_assignsubmission_onlinetext`.
- **Feedback**: submitted all 8 items (3 rated scales, 1 checkbox, 4 open text), confirmed "Your answers have been saved" and all 8 values read back correctly from `mdl_feedback_value`, `anonymous_response=2` (identified, as designed).
- **Real, unrelated finding, not fixed:** the Mastery Check quiz shows "Review not permitted" after a student finishes -- no review options were explicitly configured when the quiz was created (same as 01.1/01.3's own build scripts), so it's defaulting closed. Whether students should be able to review their own Mastery Check responses after submitting is a real policy question, not something to decide unilaterally -- flagged for Jay, not changed.
- Cleaned up all 3 test submissions from `foxcstest` afterward (`quiz_delete_attempt()`, direct `assign_submission`/`assignsubmission_onlinetext` deletes, `feedback_delete_completed()`) so the account stays a clean preview and didn't burn one of its 3 allowed Mastery Check attempts. Reverted cmid 214/215/218 back to hidden, purged caches. Final state verified: 0 attempts/submissions/completions remaining on foxcstest, all 3 cmids visible=0 again.
- Consolidated the working script as `02-authoring-system/tools/verify_01_04_submissions.mjs` (a dozen throwaway `debug-*.mjs` iterations along the way were deleted, not committed) -- reusable for the next lesson's activities by updating the cmid constants at the top.

**Bottom line for Jay: yes, all 3 save correctly and persist in the database exactly as typed.** No data-loss risk found for any of the three new 01.4 activities.

## 2026-09-03 (continued yet again) — Urgent live fix: real students were blocked from re-entering 01.4 Instruction/Practice

**Found while investigating, not from a report:** real students (13 real `g1-*` codenamed accounts, not test data) had already started working through 01.4 Instruction and Practice this morning (`lesson_timer` rows from 07:57-08:22) -- this lesson is genuinely live and in active use, not just under review. Jay reported "not allowed to retake the lesson" clicking into 01.4 Printing Output/Practice himself; both had `retake=0` (single-attempt-then-review-only), which is what was blocking re-entry once already gone through once. Jay confirmed: should be unlimited attempts, with credit tracked via completion.

**Safety-checked first, not assumed clean:** confirmed via `logstore_standard_log` that zero real students touched cmid 214/215/218 during this session's earlier visibility-toggle testing window (07:40-08:00) -- every event in that window was `foxcstest` or my own cleanup. Real student activity on Instruction/Practice started at 07:57, after my test window's activity had already stopped. No real-student exposure to the still-in-review Mastery Check/Coding Exercise/Feedback happened.

**Fixed:**
- `retake=1` on both 01.4 Instruction (lessonid=11) and Practice (lessonid=12) -- unlimited attempts, matching what Jay wants going forward (this is a real departure from 01.1's original `retake=0`+review-mode decision, made deliberately at the time; not retrofitted onto 01.1 this session, just changed for 01.4 per today's explicit instruction).
- Ran the completion finalize pass that was still outstanding for 01.4 (flagged in the 01.4 build plan, never done): `completion=2` + `completionendreached=1` on both; Instruction additionally gets `completionpassgrade=1` + grade_item `gradepass=50`, mirroring cmid=193's exact real settings. This was completely unset (`completion=0`) before now -- real students who already finished this morning should pick up completion credit once Moodle's completion check next runs for them.
- Made 01.4 Mastery Check (cmid=214) **visible**, per Jay: the quiz password is the real access gate, module visibility doesn't need to double as one. Coding Exercise (215) and Feedback (218) intentionally left hidden -- Jay only named the Mastery Check; asked whether those two should go live too now that students are already mid-lesson.

**Not yet done / open:** whether 01.1-01.3's own `retake=0` should also change to match this new standard, or whether 01.4 is a deliberate one-off given it's mid-flight with live students right now and the earlier lessons aren't. Don't retrofit 01.1-01.3 without Jay confirming which it is.

**Followed up same session:** Jay confirmed Coding Exercise and Feedback should go live too. Both flipped `visible=1`. **All 5 real 01.4 modules are now visible**: Instruction(212), Practice(213), Coding Exercise(215), Mastery Check(214), Feedback(218). Left cmid=101 (the old superseded MVP static-resource placeholder) hidden -- not real current content, intentionally not part of this.

**Next up (Python):** run `build-lesson-01-04-practice-ladder.php`, then `check-lesson-ladder-wiring.php --cmid=<result> --pool-cap=2` (zero errors/warnings bar); build the Mastery Check quiz and the Coding Exercise assignment per the plan doc; resolve the two still-open decisions in that doc (Safe Exam Browser for 01.4, due dates for 01.2/01.3/01.4); run the full verification checklist at the bottom of the plan doc before calling 01.4 live.

## 2026-09-04 — Jay's 6-week absence confirmed; real ground-truth audit of Seminar III Lessons 2-8 (all hidden); autograder and rubric scope decided. No content work done tonight, this is a planning/logging session only, per Jay's explicit "don't do more work tonight" request.

**Context:** Jay is out 2026-09-21 through 2026-10-30 (6 weeks) — confirmed against the real CPS district calendar (`starter context/EDUC_District_Calendar_...pdf`, previously only flagged as "still coming" in `open-questions.md`; that placeholder language should be reconciled/removed next time this file is touched for real). His last in-person week is 2026-09-14 to 2026-09-18. He technically returns 2026-11-02, but that's a CPS Parent-Teacher Conference day (non-attendance) and 2026-11-03 is a district holiday, so his real first day back is **Wednesday 2026-11-04**. Full detail saved to memory as `project_jay_absence_q1_pacing`.

**The load-bearing fact: this absence covers the rest of Quarter 1.** Q1 progress reports go out 2026-09-24 (3 days into the absence) and Q1 itself closes 2026-10-23 (inside the absence window). A sub will be present the full 6 weeks and can grade manually, but will not adjust pacing or content — everything needs to run on its own via due dates/completion, not live intervention.

**Real-DB ground-truth audit of `foxcs-seminar3` (course id 5), done because `CLAUDE.md`'s "Lessons 2/4/8 have full content" note turned out to describe repo files, not live Moodle state:**

- **Lesson 1 (section 2)** is the only lesson genuinely live: real H5P/native-Lesson/Quiz activities (1.1-1.10), visible, working.
- **Every single item in Lessons 2 through 8 (sections 3-9) is `visible=0`.** Nothing past Lesson 1 is actually showing for students right now, full stop — this is a much bigger gap than "some lessons are missing pieces."
- What exists for 2-8 is flat static HTML file resources uploaded via `populate-seminar3-resources.php`, not interactive/gradeable Moodle activities — no completion tracking, no due dates wired in, nothing submittable, nothing to grade.
- File-level gaps, confirmed directly against the DB and the repo:
  - **Lessons 3, 5, 7**: only a presentation + the instructional page exist. No Quick Reference, Guided Practice, Independent Practice, Check, or teacher answer key for any of the three.
  - **Lesson 6**: only a Quick Reference exists beyond the instructional page; no Guided/Independent Practice or Check.
  - **Lessons 2 and 8**: full printable-sheet sets exist as repo files, but neither has a teacher answer key yet (Lesson 4's answer key does exist, Lesson 1's does too).
  - A few resource names look like accidental duplicates worth a quick sanity check before building on top of them: Lesson 4 has both "Percent What Part Of The Whole" and "Percent" as separate resources; Lesson 6 has both "Letters That Stand For Numbers" and "Variables And Expressions"; Lesson 8 has three similarly-named resources ("My Quarter 1 Skill Story," "Quarter 1 Consolidation," "Quarter 1 Skill Story").

**Decisions made with Jay tonight, ready to build against tomorrow:**

- **Target bar for Lessons 2-8, Jay's own words:** he wants the lessons to *feel consistent* to a student, but is deliberately open on mechanism per lesson — an HTML embed of his existing adaptive content is fine, a real native Moodle Lesson is fine, a simplified version is fine too. **Don't force every lesson into the full Lesson-1-style H5P rebuild if a lighter, still-consistent mechanism gets it live faster.** Consistency of feel/structure across lessons is the actual bar, not uniformity of implementation technique.
- **Parallel build approved**: Jay's fine running one agent per lesson (or per pair) simultaneously, same pattern as the Python Unit 01 rebuild (2026-08-30, see `decisions-log.md`) — but that build's own lesson (drift between forks, stale cross-references, an em-dash sweep needed afterward) applies here too: budget a real consolidation/consistency pass across all lessons once the parallel builds land, don't skip it.
- **Autograder (`05-grader/`) is now a real near-term priority, not indefinitely deferred.** Jay wants to grade already-submitted work through it when he returns 2026-11-04, adapted to scan submissions directly out of Moodle (DB/API) rather than the original Classroom-download design — "if we are just scanning the submissions and generating feedback/assessing work then we should be good." He also wants an **effort-grade** option for interim credit on what's been completed so far, with adjustable points-possible per item, not just a strict rubric score.
- **New standing rule, applies to every course going forward:** every student-facing gradable submission needs an internal rubric/checklist in `teacher-materials/`, even if it's just a plain list of what a correct/complete answer needs — saved as `feedback_submission_rubric_requirement` in memory. Lessons 2-8 currently have nothing submittable yet, so this rule has nothing to attach to there yet, but any new submittable item built for them needs one from the start.
- **Build priority confirmed:** Jay is personally handling the Game II/Web Dev three-pathway build and Python himself. **Seminar III is the priority for this session/assistant to build** — he needs "the first quarter and some change" finished before 2026-09-21, i.e., a bit past the literal Q1 boundary (Lesson 8/9), not stopping exactly at it.
- **Also wants some postsecondary project content folded into this window** — a real scope addition against the existing plan (postsecondary work was previously concentrated in Q4 per `01_SEMINAR_III_COURSE_PLAN`/[[project_seminar3_act_exam_pacing]]). Not yet scoped which pathway(s) or what the project(s) actually are — needs a real conversation with Jay before building, not a guess.

**Explicitly not done tonight, picking up tomorrow (2026-09-05) per Jay's direct request to stop for the night:**

1. Sanity-check the handful of duplicate-looking resource names above before treating any of them as safe to build on top of.
2. Scope and confirm the postsecondary project content (which pathway, what the project actually is) with Jay directly.
3. Decide, lesson by lesson, which mechanism (H5P/native-Lesson rebuild vs. HTML-embed-of-existing-adaptive-content vs. simplified) gets each of Lessons 2-8 to "live, consistent-feeling, has at least one real gradeable+rubric-backed item, has a real due date" fastest, then dispatch parallel builds against that plan.
4. Fill the confirmed file-level gaps (Lessons 3/5/7 full printable sets; Lesson 6's missing Guided/Independent/Check; answer keys for Lessons 2 and 8) as part of whichever mechanism gets picked per lesson.
5. Flip everything visible with real due dates once built and reviewed — don't leave it hidden the way 2-8 currently are.
6. Start real scoping of the adapted `05-grader/` pipeline (Moodle-native submission scanning, effort-grade support, adjustable points-possible) — real build target for before 2026-11-04, not yet started.
