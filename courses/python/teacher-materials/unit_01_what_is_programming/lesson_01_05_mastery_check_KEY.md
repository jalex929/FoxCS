# 01.5 Mastery Check: Teacher Key

**Never include this file in the folder distributed to students.**

**1. (DOK 1-2. Direct prediction)** Exactly:
```
Level 5
Keep going!
```
The commented-out `Level 6` line produces no output. Deterministic, auto-checkable once the grader exists.

**2. (DOK 2. Apply the weak-vs-strong comment distinction)** No single correct wording. Full credit requires the rewrite to state a *reason* the line exists (e.g. `# pause the game so the player can check the settings menu without losing progress`), not just describing the code again in different words (e.g. `# this displays "Game paused" on the screen` is still not full credit. It's a restatement, not a reason).

**3. (DOK 3. Refute a misconception with reasoning)** Expected: disagree. Comments do something real even though Python ignores them. They communicate to *humans* (the original programmer later, teammates, a future maintainer), which is a different and equally real audience from "the computer." Full credit requires naming that human-audience distinction specifically, not just asserting "comments are useful" without saying for whom or why.

**Common misconception to watch for (`CODE-01`):** treating "Python ignores it" as equivalent to "it doesn't matter". This conflates the computer's audience with the only audience, which is exactly what item 3 is testing for.
