# 2.0 Adaptive Practice — Full Question Bank

**Not applicable to this lesson.** 2.0 Getting Ready for Certification is a one-time orientation step (Certiport/GMetrix account setup + VS Code setup), not a skill lesson — there's no assessed skill here for adaptive Reinforce/Core/Extend practice to attach to, and no Mastery Check or Project rubric either (see `01_instruction.html`'s own header comment).

This file exists anyway, per Jay's standing rule that every lesson gets one of these — so a future pass through `content/unit_02_variables_and_data/` doesn't have to re-derive whether 2.0 was skipped on purpose or just missed.

Completion for this lesson is tracked as a single manual-completion checkbox (real accounts created + form submitted + VS Code set up), not a graded skill check — see `01_instruction.html`'s `markDone()` function and `../../../../../02-authoring-system/telemetry-and-analytics.md`'s Live Implementation section for how that's logged.
